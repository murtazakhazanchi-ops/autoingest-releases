Read every file in this project. Then update CODEBASE_OVERVIEW.md to reflect the current state of the codebase. Do not change the structure or section headings. Only update the content within each section to match what is actually in the code now. Add any new files, functions, IPC channels, state variables, or data flows that did not exist before. Remove anything that no longer exists. Save the updated file as CODEBASE_OVERVIEW.md.
