# Changelog — docs/product/

This is a log of changes to the **documentation system itself** (registry entries added/changed, roadmap milestones moved, major restructuring) — not a user-facing release changelog. For user-facing release notes, see `docs/release-notes-*.md`.

Append newest first. Never edit or delete a prior entry — if something was wrong, add a correcting entry.

---

## 2026-08-14 — Knowledge Portal Stage 2 merged to `main`; event-coordination reconciliation (AI-FEAT-058 / AI-RM-011 / DEC-020)

- **Task type**: merge + post-merge historical/documentation closure, plus a mid-closure product-owner clarification reconciled in the same pass.
- **Merge**: `knowledge-portal-stage2` (13 commits, 58 files, tip `d89440b`) merged into `main` via a real two-parent merge commit (`765e9b8`, parents `56cfd1b`/`d89440b`) — history preserved, not squashed. Every gate (354 test assertions, `build`/`validate`, `git diff --check`, the 119-question corpus, the live Registry acceptance script, deterministic-rebuild hash, zero-runtime-code-touched, `127.0.0.1`-only binding) independently re-verified from the actual merged `main` state in a fresh temporary worktree.
- **Mid-closure reconciliation**: a product-owner clarification arrived during this same closure pass, correctly identifying that the Online Registry's primary purpose is distributed event coordination between separated operators without a shared NAS — not merely presence — and requiring a forensic, evidence-only trace before any documentation changed. The trace (20 specific implementation questions, each independently verified in source) found this capability substantially **VERIFIED IMPLEMENTED end-to-end**, a materially larger scope than `AI-WF-006`'s prior version had documented — a real coverage gap in this session's own earlier Stage 2 work, found and closed the same day, not a case of previously-documented behavior turning out to be false. A related, narrower gap was also found and flagged (not rewritten, out of scope) in `AI-FEAT-048`'s own pre-existing documentation via an append-only update to `11_ARCHITECTURAL_EVOLUTION.md`.
- **New/updated records**: `AI-WF-006` substantially expanded (fifth first-class Registry concept, 20-question forensic classification table, evidence-marked operator workflow diagram); new `test/knowledgeEventCoordination.test.js` (22 tests); [AI-MEM-0004](memory/AI-MEM-0004_KNOWLEDGE_PORTAL_STAGE_2_OPERATOR_KNOWLEDGE_ARCHITECTURE.md) (new — full Stage 2 engagement narrative, Stage 1 audit through this closure); `AI-FEAT-058` (Merged to Main + Post-Merge Event-Coordination Reconciliation sections); `DEC-020` (Post-Decision Evolution note); `AI-RM-011` (status: Completed); `04_PROJECT_DASHBOARD.md` (`Completed roadmap milestones` field updated to keep the generated dashboard's own cross-check green); `11_ARCHITECTURAL_EVOLUTION.md` (append-only update note on AI-FEAT-048's scope).
- **Three real regressions found and fixed during this same pass**, before being accepted, not shipped and found later: a coincidental keyword collision (the word "make," added by the `AI-WF-006` expansion, briefly out-ranked the correct event-creation workflow for "how do i make an event" — reworded, not patched around) and two boundary-trigger phrasing gaps the new adversarial checks in `knowledgeEventCoordination.test.js` caught immediately (widened, not loosened — same discipline as every earlier Stage 2 phase).
- **Verified**: full product-docs suite green throughout (354+ assertions), `knowledgeEventCoordination.test.js` 22/22, the pre-existing 119-question corpus unaffected in aggregate (two entries legitimately improved weak→strong, documented as such), `validate` 0 errors at every checkpoint.
- **Evidence confidence**: every forensic classification traces to a specific file/line in source, re-verified by direct code reading, not inferred from the product-owner's own stated intent (per explicit instruction). No LLM, embeddings, live Registry integration, production Electron integration, or new release was introduced. Stage 3 has not begun.

---

## 2026-08-13 — AutoIngest Knowledge Engine, Stage 2 Phases 4-24 (AI-FEAT-058 / AI-RM-011 / DEC-020)

- **Task type**: large, multi-phase capability extension, approved after a dedicated Phase 0-3 read-only audit (operator intent taxonomy, knowledge/workflow schema proposal, Online Registry architecture findings, retrieval architecture design) was presented and explicitly approved, preserving the audit's four Online Registry distinctions (presence/activity/conflict/locking), the Capability-vs-Workflow architecture, and the explicit `conflict:warning` dormant-capability constraint.
- **What happened, phase by phase**: (Phases 4-19, prior session) a curated Intent/concept-synonym retrieval layer (`lib/intentConcepts.js`) and question-type classifier (`lib/questionClassifier.js`), both run through the existing unchanged `lib/query.js` ranker; a new canonical Workflow record type (`AI-WF-###`, 8 authored records under `docs/product/workflows/`, distinct from Capability — see [DEC-020](decisions/DEC-020_STAGE_2_KNOWLEDGE_ARCHITECTURE_WORKFLOW_RECORDS_AND_CONCEPT_LAYER.md)); roadmap/dashboard question routing. (Phase 20-21) expanded the eval corpus from 20 to 119 questions (`lib/knowledgeTestCorpusV2.js`) with substantial Online Registry/teamwork/offline coverage, finding and fixing 5 real defects — most seriously, several Registry-authority questions (including "will there be a conflict warning?") resolving AVAILABLE in direct violation of the approved `conflict:warning` constraint; fixed via 5 new curated boundaries plus a `hardOverride` precedence mechanism. Added `test/knowledgeHallucinationV2.test.js` (10 tests) as a permanent regression gate. (Phase 22-23) extended the local portal from 3 tabs to 9 (Getting Started, Workflows, Teamwork & Live Activity — documentation-only, explicitly never live — Status, Troubleshooting, Directory/onboarding browsing mode), with 3 new read-only API endpoints, each a direct passthrough of already-generated data. (Phase 24) a fresh adversarial review — 36 newly-invented questions, verified distinct from both corpora — found and fixed 6 more real defects: compound "since X, does that mean Y?" questions bypassing established boundaries (fixed by extending `hardOverride` to all 6 original Stage 1 boundaries, not just the 5 Registry ones), a substring-collision bug in a concept-hint trigger, a boundary-recall gap, and an EXPLANATION-classification gap whose fix also corrected 3 pre-existing weak/missing answers once re-verified against the full corpus. Added `test/knowledgeAdversarialPhase24.test.js` (8 tests).
- **Total real defects found and fixed across Phases 20-24, all before being accepted into the corpus or shipped**: 11, each root-caused and fixed with the smallest correct change, none "documented around." A smaller number of genuine, structural limitations (no stemming, no typo tolerance, no compound-claim verification — inherent to keyword-overlap scoring, not something this stage's scope covers) were found and deliberately left disclosed rather than patched with question-specific exceptions, per the brief's own explicit instruction.
- **Final verified state**: `knowledge.test.js` 18/18, `knowledgeHallucinationV2.test.js` 10/10, `knowledgeAdversarialPhase24.test.js` 8/8, full pre-existing product-docs suite unaffected throughout every commit, `validate` 0 errors at every checkpoint, combined 119-question corpus 100/119 exact pass + 19/119 documented known limitations + 0/119 unexplained.
- **New/updated records**: [AI-FEAT-058](features/AI-FEAT-058_AUTOINGEST_KNOWLEDGE_ENGINE_STAGE_1.md) (Stage 2 evolution sections for Phases 4-24), [DEC-020](decisions/DEC-020_STAGE_2_KNOWLEDGE_ARCHITECTURE_WORKFLOW_RECORDS_AND_CONCEPT_LAYER.md), [AI-RM-011](02_MASTER_ROADMAP.md#ai-rm-011-autoingest-knowledge-onboarding-portal-stage-1-stage-2) (status/deliverables/acceptance criteria updated to reflect Stage 2), 8 new `AI-WF-###` Workflow records. No `BUG-###` records created — every defect above was caught and fixed pre-commit during this feature's own testing, with no shipped/user-facing incident, per `docs/product/CLAUDE.md` § 12's same reasoning already applied to Stage 1.
- **Explicitly not done, per the approval's own stop condition**: no LLM, no embeddings, no live Registry dashboard integration, no production Electron integration, no new AutoIngest release, no Stage 3 work. Phases 25-27 (this entry, plus the two mandatory Stage 2 report sections) remain in progress in the same session.
- **Evidence confidence**: real local test runs at every commit, direct source citation for every architectural/boundary claim, every defect independently reproduced via a diagnostic script before being fixed and re-verified after. No application runtime code was modified.

---
## 2026-08-13 — AutoIngest Knowledge Engine, Stage 1 prototype (AI-FEAT-058 / AI-RM-011)

- **Task type**: new capability, single implementation session, following on directly from the completed Phase 1 Knowledge & Onboarding Portal audit.
- **What happened**: built the smallest working slice proving AutoIngest's existing `docs/product/` system — the feature registry and `scripts/product-docs/lib/query.js`'s existing deterministic offline search — can power a grounded, citation-honest, natural-language answer engine with no LLM, no embeddings, and no hosted service. New generated artifact `docs/product/generated/knowledge-index.json` (57 records, DOCSYS/schema version bumped 1.2.0 → 1.3.0, additive only). New `knowledge <sub>` CLI command (`ask`/`eval`/`serve`). New minimal local static+API portal (`scripts/product-docs/knowledge-portal/`), a Node-core-only HTTP server bound to `127.0.0.1`, no new npm dependency.
- **Re-verification, not assumption**: Stage 1's brief explicitly required re-checking Phase 1's proposed implementation against current repository state before building on it — confirmed `lib/query.js`'s ranking table, `lib/featureIndex.js`'s record shape, and `automation/contextEngine.js`'s existing `task`/`explain` routing directly from source rather than trusting the audit's prose description.
- **Four real defects found and fixed during the feature's own construction, before commit** (full detail in AI-FEAT-058's Evolution Journal): (1) an exact-match bug-status regex miscounted `BUG-006`'s "Fixed (recurring pattern — see Prevention)" as still-open; (2) generic single-keyword ties (e.g. "event", "import", "archive") could present a low-relevance record as a confident answer — added a local `matchQuality`/hedge mechanism; (3) "Does AutoIngest offer cloud backup?" would have reported the architecturally-excluded capability as available via a coincidental keyword match — reordered the answer logic so the curated `NOT_SUPPORTED` boundary table is checked before a merely-weak match is allowed to stand; (4) self-inflicted — this record's own original title ("AutoIngest Knowledge Engine...") made "autoingest" one of its own search keywords, causing it to keyword-match nearly every question in the eval corpus; fixed by renaming to "Knowledge Engine (Stage 1 Prototype)", matching every other feature title's convention of never restating the product's own name.
- **20-question eval corpus** (`lib/knowledgeTestCorpus.js`): 18/20 passed exactly as expected; 2/20 deviated in an already-documented, evidenced way (both recorded in the corpus file's own `knownLimitation` field — not silently absorbed); 0/20 unexplained.
- **New records**: [AI-FEAT-058](features/AI-FEAT-058_AUTOINGEST_KNOWLEDGE_ENGINE_STAGE_1.md), [AI-RM-011](02_MASTER_ROADMAP.md#ai-rm-011-autoingest-knowledge-onboarding-portal-stage-1-stage-2), [DEC-019](decisions/DEC-019_KNOWLEDGE_ENGINE_REUSES_EXISTING_RETRIEVAL_NO_NEW_SEARCH_SYSTEM.md). No `BUG-###` record created — the three defects above were caught and fixed pre-commit with no user-facing incident (recorded in AI-FEAT-058's Evolution Journal instead, per `docs/product/CLAUDE.md` § 12).
- **Also discovered, out of this feature's scope to fix**: AI-FEAT-001's "Related technical docs" field cites a nonexistent `docs/README.md` (found by this feature's own new source-resolution test, `scripts/product-docs/test/knowledge.test.js` check 5) — the existing `checkRelatedTechnicalDocs` validator only checks for an empty field, never whether a cited path resolves. Not fixed here (rewriting the 57 existing feature records is out of Stage 1's scope); documented as a known exception in the new test and flagged here for whoever next touches `14_VALIDATION_SPECIFICATION.md` or AI-FEAT-001.
- **Verified**: `scripts/product-docs/test/knowledge.test.js` 17/17 (10 Phase-12-checklist assertions + 7 dedicated negative/hallucination tests); full existing suite unaffected, `node --test scripts/product-docs/test/*.test.js scripts/product-docs/test/automation/*.test.js` → 33/33 files passing; `node scripts/product-docs/cli.js validate` → 0 errors, 15 warnings, 14 information, 56 evidence gaps (unchanged baseline — no regression); a live local-server smoke test (`knowledge serve`, `curl` against `/`, `/api/capabilities`, `/api/roadmap`, `/api/ask`).
- **Evidence confidence**: real local test runs, real live-server HTTP responses, direct source reading of every module reused. No application runtime code was modified.

---

## 2026-08-13 — Part 9 live-pilot closure: real Windows-client verification (AI-FEAT-057 / AI-RM-010)

- **Task type**: live-pilot closure (one additional RC publication; no application code changed; documentation-only reconciliation).
- **What happened**: published `v0.9.12-rc.3` — deliberately built from the exact same application source as `rc.2` (zero application-code diff, independently confirmed via `git diff --stat` and via `electron-builder`'s own `build.files` packaging allowlist, which excludes `docs/**`/`scripts/**`/`.github/**` entirely) — solely to prove Preview→Preview automatic update, since `rc.2` was already the newest Preview release and couldn't exercise that path itself. First publish attempt (targeting rc.2's own commit directly) failed on a GitHub `workflows`-permission push-protection unrelated to the channel architecture (root-caused precisely, then resolved by dispatching from `main`'s tip instead — see AI-FEAT-057's Evolution Journal).
- **Real Windows-hardware evidence returned**, across two tester sessions: Stable isolation (v0.9.11 never discovered rc.2), manual bootstrap install (data/settings preserved, channel correctly defaulted to Stable), Preview↔Stable persistence, Preview→Preview automatic discovery/download/install (independently evidenced by an in-app download-progress screenshot, not a manual install), and no downgrade to v0.9.11 at any point the channel was switched back to Stable while running a newer RC.
- **Forensic bootstrap audit**: a prior tester-instructions draft incorrectly assumed the already-installed v0.9.11 had Part 9's Update Channel selector. Verified from git history that v0.9.11 predates Part 9 entirely (`git merge-base --is-ancestor v0.9.11 dfb9180`) and has no code path capable of ever self-selecting Preview — `electron-updater`'s `allowPrerelease` is fixed permanently at construction time from the binary's own compiled-in version string. Classified as a test-plan omission, not an architecture defect: manual one-time RC installation is a real, safe, already-functional bootstrap for designated testers (proven via identical `appId`, identical `<userData>/settings.json` path, and correct default-to-Stable behavior post-bootstrap). No code change made.
- **Acceptance Matrix**: added to AI-FEAT-057 — 22 claims, each classified into one of five evidence tiers (real client / live CI-GitHub / automated test / source-SemVer / not verified). Every claim in this feature's own scope is now backed by real evidence except one correctly-deferred item (a real future Stable release superseding a running RC — requires publishing Stable, out of scope for this pilot).
- **Non-blocking finding**: the tester could not confidently confirm the exact in-app version string (including the `-rc.3` suffix) — Windows' own file-properties dialog has no field for a SemVer prerelease suffix, and the app's existing title-bar version display uses a preload-context `require()` whose reliability under this app's `sandbox: true` setting is genuinely uncertain (wrapped in a silently-swallowing `try/catch`). Recorded as a non-blocking UX enhancement candidate in AI-FEAT-057's Future Enhancements, pointing at an already-proven, already-used alternative (`getAppVersion` IPC) in the same codebase. Not implemented — not part of this feature's approved scope.
- **Status**: AI-RM-010 updated from "Completed (logic-level; live pilot deferred)" to "Completed — verified on real Windows hardware." No new BUG/DEC records created (the earlier "downgrade" report was a tester mis-statement, corrected by the tester themselves before this closure — no defect exists, no investigation was performed on the basis of the miswritten report, per explicit instruction).
- **Evidence confidence**: real Windows-hardware execution (screenshots, described observed behavior), real GitHub API state, real CI run logs, direct git-history proof — the highest evidence tier this documentation system supports.

---

## 2026-08-13 — Fixed Product Documentation Validation CI failure (BUG-016)

- **Task type**: small, targeted CI/tooling fix (systematic-debugging process: reproduced deterministically pre- and post-fix in a `node_modules`-less `git worktree` matching the real CI condition, not guessed).
- **What broke**: `scripts/product-docs/lib/updateChannelModel.js` (added during Part 9) called `require('semver')`, an npm package never declared in `package.json` — it only resolved locally because it was hoisted as a transitive dependency of the Electron app's own `node_modules`. `product-docs.yml`'s CI job deliberately never runs `npm install`, so a fresh checkout has no `node_modules` at all, and the require failed with `MODULE_NOT_FOUND`. New record: [BUG-016](bugs/BUG-016_UNDECLARED_NPM_DEPENDENCY_IN_PRODUCT_DOCS_TOOLING_MASKED_BY_LOCALLY_HOISTED_NODE_MODULES.md).
- **Fix**: replaced the one `semver` API used (`prerelease()`) with a minimal local reimplementation, cross-checked against the real package's output across 20 cases before substituting in. Commit `fed471d`.
- **Verified**: local suite 32/32, and — the actual bar for this fix — GitHub Actions run `31668341182` itself green (`tests: 32, pass: 32, fail: 0`), not just local.
- **Evidence confidence**: real CI run logs, deterministic reproduction in an isolated `node_modules`-less environment before and after the fix.

---

## 2026-08-13 — Part 9 live CI pilot: RC published, Windows-shell bug found and fixed (AI-FEAT-057 / BUG-015)

- **Task type**: live infrastructure pilot (no application code changed beyond a one-line CI fix), documentation reconciliation from real evidence.
- **What happened**: triggered the real `rc-build` `workflow_dispatch` path for the first time. `v0.9.12-rc.1` (source commit `a507c47`) partially published — macOS assets succeeded, Windows publish failed with `ENOENT` on a literal file named `.publish.releaseType=prerelease`. Root cause: `windows-2022` runners default to `pwsh`, which mangles the single-token `-c.publish.channel=rc` electron-builder CLI override into two tokens. New record: [BUG-015](bugs/BUG-015_WINDOWS_RUNNER_DEFAULT_POWERSHELL_SHELL_MANGLES_ELECTRON_BUILDER_DOT_NOTATION_CLI_OVERRIDES.md).
- **Fix**: added `shell: bash` to the affected step (commit `66469f5`) — the same shell every other step in that job already used. Also added `shell: bash` to the equivalent macOS step for consistency (behaviorally a no-op there, since macOS runners already default to bash) per two independent review agents' recommendation.
- **Re-verified live**: `v0.9.12-rc.2` (source commit `66469f5`) published cleanly on both platforms — 12 assets, `rc.yml` + `rc-mac.yml`, `prerelease:true`. Confirmed via GitHub API throughout: `/releases/latest` never stopped resolving to `v0.9.11`; Stable's `latest.yml`/`latest-mac.yml` byte-identical before and after both RC runs (including the failed one) — isolation held under a real partial-failure condition, not just in the happy path.
- **`v0.9.12-rc.1` disposition**: left published as-is (mac-only, partial) — not deleted or altered, per the rollback runbook now formalized in AI-FEAT-057's own "Rollback / Recovery Runbook" section. Serves as real evidence of the "partially uploaded RC" recovery scenario.
- **Promotion gate dry run**: `release gate --channel stable` run locally (no `v0.9.12` tag created) against the verified RC commit — zero-drift case correctly `PASS`ed, unrelated-drift case correctly `BLOCK`ed (51 files listed). No Stable version was published.
- **Review**: `security-reviewer`, `autoingest-architect`, `code-reviewer` run against the real pilot diff — no CRITICAL/HIGH findings. One accepted structural note (no atomic dual-platform publish gate — recorded in AI-FEAT-057, not a DEC-record, since no real alternative was weighed).
- **Outstanding**: real-installed-client verification (Windows machine actually running the updater against Stable vs. Preview channel) not performed this session — tester instructions produced, results pending. Part 9 status: READY FOR ACTIVATION — WAITING FOR REAL CLIENT VERIFICATION, not yet ACTIVATED AND VERIFIED.
- **Evidence confidence**: real GitHub API state (releases, assets, channel `.yml` content), real GitHub Actions run logs, direct comparison of Stable metadata before/after — not simulated.

---


## 2026-08-12 — Part 9: Multi-Channel Release & Update System (AI-FEAT-057 / AI-RM-010)

- **Task type**: new feature, architecture. New records: [AI-FEAT-057](features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md) (subfeature of AI-FEAT-006), [AI-RM-010](02_MASTER_ROADMAP.md#ai-rm-010-multi-channel-release-update-system) (a parallel infrastructure track, not part of the AI-RM-001…009 archive-capability sequence), [DEC-017](decisions/DEC-017_STABLE_RELEASES_REBUILD_FROM_VERIFIED_RC_SOURCE_NEVER_PROMOTE_EXACT_BINARIES.md).
- **Motivation**: directly follows the v0.9.11 release-process incident ([PM-002](postmortems/PM-002_V0_9_11_FIRST_PUBLICATION_ATTEMPT_PRODUCED_AN_EMPTY_GITHUB_RELEASE.md)) — a real, public but empty GitHub Release caused by `package.json`'s version drifting from the pushed tag. Formalizes release/update infrastructure into three isolated channels rather than leaving RC/tester builds and Stable sharing one implicit path.
- **Architecture**: verified directly from installed `electron-updater@6.8.3`/`electron-builder@26.8.1` source, not assumed. Stable isolation is structural (a Stable client resolves updates via `GET {repo}/releases/latest`, which excludes every prerelease release by GitHub's own API definition) rather than convention-based. RC promotion to Stable rebuilds from the verified RC's exact source commit (only the version string differs) rather than re-uploading binaries — see DEC-017 for the investigated, evidenced reason exact-binary promotion is unsafe (the app's own version metadata is baked into the artifact at build time in three places).
- **Implementation**: `.github/workflows/release.yml` restructured — `create-release`/`build-mac`/`build-windows` (Stable) left byte-for-byte unchanged; `windows-tester-build` renamed to `development-build` (behavior unchanged); new `rc-tag`/`rc-build-windows`/`rc-build-mac` jobs. `services/settings.js`/`main/main.js`/`main/preload.js` — new `updateChannel` preference (`"stable"` default, `"preview"` opt-in). `services/autoUpdater.js` — `applyChannelSetting()` applies the preference before the first update check. `renderer/index.html`/`renderer/renderer.js` — Settings modal Update Channel toggle (deliberately its own CSS/JS class, not reusing the theme radio group's class, to avoid a real collision with the existing generic theme-radio selectors). `scripts/product-docs/automation/releaseIntelligence.js` — `checkChannelReleaseGate()` (hard-blocking version/tag/lockfile/source-drift/blocking-bug checks) and QA-checklist/promotion-readiness additions to the existing release-draft builder. `scripts/product-docs/lib/updateChannelModel.js` — new pure model of electron-updater's release-resolution logic, for testing without Electron.
- **Tests**: `scripts/product-docs/test/automation/updateChannel.test.js` (19 assertions covering all 9 required isolation/gate scenarios plus `--auto-rc-commit` discovery). `releaseIntelligence.test.js` was not modified in this session (its own 10 assertions, from the prior PM-002 repair session, cover `checkVersionTagAlignment` only). Full Part 4/5/8/9 tooling suite (32 files) passing.
- **Scope boundary**: the live pilot (an actual `workflow_dispatch` RC run) is explicitly deferred pending separate authorization — see AI-FEAT-057's Future Enhancements and the Part 9 final report. `applyChannelSetting()`'s two property assignments on the real `electron-updater` singleton could not be exercised by a pure-logic test (the library requires a real Electron process at construction time) — verified instead by direct reading of its setter source, consistent with this repository's existing testing boundary for Electron-dependent files.
- **Evidence confidence**: code-level implementation + regression tests + direct verification against installed library source (not memory, not external documentation).

---

## 2026-08-11 (release incident) — v0.9.11's first publication attempt was empty; repaired and permanent release-gate added

- **Task type**: release-process incident + corrective tooling. New record: [PM-002](postmortems/PM-002_V0_9_11_FIRST_PUBLICATION_ATTEMPT_PRODUCED_AN_EMPTY_GITHUB_RELEASE.md).
- **Incident**: the first `v0.9.11` git tag was pushed while `package.json` still read `0.9.10`. `electron-builder` derives its publish target and artifact names from `package.json`'s version, not the git tag, so it built `0.9.10`-named artifacts and attempted to publish them against the already-published `v0.9.10` release; its own overwrite-protection guard silently skipped every upload. Net result: a real, public `v0.9.11` GitHub Release with zero assets. `v0.9.10` was not modified. No client could discover the empty release since no `latest.yml`/`latest-mac.yml` was ever published for it — no production update-corruption risk existed at any point.
- **Repair**: `package.json`/`package-lock.json` bumped to `0.9.11` (commit `7bb6017`); the empty `v0.9.11` release and its tag deleted (`v0.9.10` re-confirmed intact before and after); a fresh, correctly-annotated `v0.9.11` tag created on the version-corrected commit and pushed, re-triggering the release workflow. Rebuild verified end-to-end: correct `0.9.11`-named artifacts, real (non-skipped) uploads for both platforms, correct `latest.yml`/`latest-mac.yml` metadata, 12 assets total.
- **Permanent corrective action**: `node scripts/product-docs/cli.js release gate --tag <vX.Y.Z>` (`releaseIntelligence.checkVersionTagAlignment()`, commit `e634d9a`) checks `package.json`'s (and `package-lock.json`'s) version against a target release version before a tag is created, exiting non-zero on mismatch. 4 new regression tests, including a direct reproduction of this incident's exact mismatch.
- **Cross-links**: [AI-FEAT-006](features/AI-FEAT-006_APPLICATION_AUTO_UPDATE.md) (Related postmortems + Known Bugs sections updated); [ENG-CONV-0003](conversations/ENG-CONV-0003_WINDOWS_NAS_EVENT_MANAGEMENT_FAILURE_INVESTIGATION_ROOT_CAUSE_VERIFICATION_AND_R.md) (Outcome section records the actual publication result).
- **Not a product defect**: this is a release-process gap, not an Event Management bug — BUG-011 through BUG-014's statuses are unchanged.
- **Evidence confidence**: explicit (GitHub Actions run logs quoting the exact `skipped publishing`/`existing release published more than 2 hours ago` lines, GitHub Releases/Tags API responses before and after repair, regression tests reproducing the exact mismatch).

---

## 2026-08-11 (pre-merge documentation closure) — BUG-011: canonical Root Cause section corrected to lead with the confirmed final defect

- **Task type**: documentation correction only, no code change. During the final pre-merge audit (Phase 4 documentation closure) ahead of merging `fix/windows-event-management-rc` into `main`, found that BUG-011's top-level `## Root Cause` section still stated only the **original** 2026-08-07 hypothesis (transient `fsp.readdir()` failures collapsing into an empty-events shape) — a real, separately-fixed defect, but not what the real Windows/NAS tester actually hit. The true final root cause (mixed `sequence` runtime type crashing `resolved.sort()`, confirmed 2026-08-11) was only stated inside the Investigation Log and Fix section, never promoted to the canonical summary a reader sees first.
- **Correction**: `## Root Cause` now leads with the confirmed final defect (mixed `sequence` type — string from `parseEventName()` vs. number from `eventJson.sequence` — crashing `resolved.sort()`'s `localeCompare` call after filesystem discovery and event.json parsing had already succeeded for every entry), and retains the original 2026-08-07 hypothesis below it, explicitly labeled as investigated-and-fixed-but-not-the-tester's-actual-symptom — per this system's append-never-erase policy (§11), nothing was deleted.
- **Verified during the same pass**: BUG-012, BUG-013, BUG-014 status fields already accurately distinguish hardware-verified (BUG-013, with a specific cited tester statement) from fix-applied-but-hardware-evidence-pending (BUG-012, BUG-014, each with an explicit note on why the tester's session never reached that code path) — no correction needed for those three.
- **Evidence confidence**: direct re-reading of the full BUG-011 record against the real investigation timeline already documented within it.

---

## 2026-08-11 (final stabilization pass) — L1–L7 closed: Canonical Representation Audit findings resolved before stable release

- **Task type**: bugfix (L1) + hardening (L2, L3, L5) + deduplication (L6) + rename (L7) + investigation (L4, no change). Closes every finding from the standalone Canonical Representation Audit. Zero new confirmed production bugs found or introduced.
- **L1 (HIGH)**: 20 main-process path-containment sites (broader than the audit's original 8 — a full sweep found 12 more of the same pattern plus 2 `path.relative()`-based sites) replaced with `renderer/pathUtils.js`'s `isPathUnderRoot()`/new `isPathUnderOrEqualToRoot()`, shared into `main.js` via `require()` — the same dual-export pattern BUG-013 established. Every site kept its existing `path.resolve()`/`fsp.realpath()` traversal-safety call; case-folding layers on top, doesn't replace it. `isPathUnderRoot()`'s own behavior confirmed byte-for-byte unchanged (14/14 prior assertions pass unmodified).
- **L2 (MEDIUM)**: `transferImportService.js`'s checkpoint numeric fields normalized once, on read only — the persisted file format is never touched.
- **L3 (MEDIUM)**: photographer-sequence read-side comparator now normalized at one point, matching the write-side guard; non-coercible values safely fall back to "unsequenced" instead of reaching the comparator as `NaN`.
- **L4**: investigated — traced to `services/localMirrorService.js`'s `ensureLocalMirror()`; the flagged forward-slash-normalization pattern is used pervasively across `renderer.js` (dozens of sites, including the everyday local-first import path already exercised in real Windows testing) and Node's fs layer accepts forward-slash UNC paths on Windows. Documented as harmless, left unchanged. The *original* app.log evidence that raised L4 traced to a different, already-correct code path and remains formally unexplained.
- **L5 (LOW)**: `metadata:getStatus`'s IPC response now projects out `_context`/`_resolved`; `metadata:retry`'s internal (non-IPC) use of the raw `_context` is untouched. `getMetadataStatus` currently has zero renderer callers — a structural fix for a not-yet-live consumer.
- **L6 (LOW)**: `seqPrefix()` deduplicated into `renderer/photographerSequenceUtils.js` (same sharing pattern as L1/pathUtils.js) — `services/photographerSequenceService.js` re-exports it by reference, zero external call-site changes. An adjacent unused duplicate (`_SEQ_EVENT_ROOT_KEY`, dead code) removed rather than also shared.
- **L7 (LOW)**: `main/aliasEngine.js`/`main/listManager.js`'s colliding `normalize()` functions renamed to `normalizeForAliasMatch()`/`normalizeListEntry()` — pure rename, zero external consumers of either by the old name, confirmed by repo-wide grep before the change.
- **Tests**: 7 new test files (`l1PathContainmentRegression`, `l2CheckpointNumericNormalization`, `l3PhotographerSequenceNormalization`, `l5ExifBatchContextSanitization`, `l6SeqPrefixDeduplication`, `l7NormalizeNamingCollision`, plus 8 new assertions in `pathUtils.test.js`) — 42 new assertions total. Full existing suite (24 files) re-run and passing unchanged.
- **Repository guarantees verified**: exactly one path-containment implementation; exactly one `seqPrefix()` implementation; no new duplicated normalization logic introduced; no behavioral regressions (confirmed by full suite + `git diff --check`).
- **Evidence confidence**: code-level implementation + regression tests proven against the real modified code (not mirrors, except where a private renderer function made a live UI-driven test impractical — L3, L5 partially — documented as such in each test file).

---

## 2026-08-11 (verified + cleanup) — BUG-011: real-Windows confirmation, 37-vs-36 audit closed, diagnostics cleaned up for stable release

- **Task type**: verification + repo-wide audit + diagnostics cleanup, no functional behavior changed. A real Windows/NAS `app.log` and screenshot confirmed the sequence-normalization fix (previous entry) working: `1448-01-11 _UK Safar` now lists 36 real events, no crash, across multiple scan attempts.
- **37-vs-36 audit**: closed as expected behavior. `events.length` (37) always equals `resolved.length` (36) + `unparseable.length` (1) by construction — the 37th item is a specific, named, correctly-classified folder (`01-QMZ`, no `event.json`, unparseable name) that lands in "Unrecognised Folders", not "Existing Events". No code change.
- **Repository-wide sequence-type audit** (Phase 5, read-only): confirmed exactly three `.sequence.localeCompare()` sites exist codebase-wide — the just-fixed `_scanEventsCore`, the already-correct `_scanNasArchive`, and `renderer/eventCreator.js`'s pending-sync re-sort (safe by inheritance from the fix, not an independent risk). No other live mixed-type bug of this class found.
- **Diagnostic instrumentation cleanup** (Phase 4): removed the exhaustive per-entry/per-operation trace logging built up across the whole stall investigation — per-entry START/OK markers, the 10-second heartbeat, the per-entry `RECORD` dump, `OPERATION_TIMING`, and the IPC-boundary happy-path markers (`IPC_HANDLER_ENTER`/`SCAN_PROMISE_CREATED`/`SCAN_PROMISE_RESOLVED`/`BEFORE_IPC_RETURN`/`RENDERER_SCAN_INVOKE_START`/`RENDERER_SCAN_INVOKE_RESOLVED`) — now that the root cause is confirmed and fixed, matching the code's own original stated intent to remove this layer once resolved. **Retained**: `EVENT_DISCOVERY_SUMMARY`, `SCAN_COMPLETE`, `UNEXPECTED_EVENT_REJECTION`/`RENDER_COUNT_MISMATCH` assertions, `DIRENT/STAT MISMATCH`/`DIR_ENTRY_TYPE_MISMATCH`, concurrent-scan detection, and both IPC error-path loggers (`IPC_HANDLER_ERROR`/`RENDERER_SCAN_INVOKE_ERROR` — the exact mechanism that caught the real bug). Net: ~93 lines removed from `main/main.js`, ~10 from `renderer/eventCreator.js`. `test/bug011ScanStallInstrumentation.test.js` removed (tested now-nonexistent scaffolding); `test/bug011IpcBoundaryAudit.test.js` updated to assert retained markers still fire and removed ones are gone.
- **Tests**: full suite re-run and passing unchanged; sequence-normalization fix independently confirmed unaffected by the cleanup.
- **Documentation**: AI-MEM-0003 reopened with an evolution-timeline entry summarizing this continuation. BUG-011 remains "Fixed — core defect confirmed on real hardware"; formal "Verified" status awaits the consolidated BUG-011–014 acceptance pass.
- **Evidence confidence**: explicit (real tester `app.log` + screenshot) + code audit + regression tests.

---

## 2026-08-11 (root cause confirmed) — BUG-011: mixed `sequence` type crashes master:scanEvents's sort — fixed and regression-tested

- **Task type**: bugfix. The real Windows tester's `app.log`, captured via the IPC-boundary instrumentation shipped the prior round, caught the actual exception: `TypeError: b.sequence.localeCompare is not a function`, thrown from `master:scanEvents`'s `resolved.sort()` after all 52 entries were successfully discovered. Platform-independent JS type coercion, not a filesystem/IPC/serialization defect — every hypothesis from the two prior rounds was correctly ruled out; the crash simply lived one step further than any of them looked.
- **Root cause**: `sequence` is a zero-padded string when derived from `parseEventName()`'s regex (the common case), but falls back to `eventJson.sequence` — whatever type was last persisted to disk — when a folder's own name fails to parse (e.g. an unrecognized city token). The Create/Edit Event form always writes `sequence` as a number (`parseInt(seq, 10)`), so a single unparseable-name folder mixes a number into an otherwise all-string sequence set; the very next sort comparison against a normally-parsed neighbor throws, aborting discovery for the *entire* collection.
- **Producer audit**: `parseEventName()` → string; Create Event form → number (unmodified through to disk); Edit Event workflow → pass-through, unchanged; `normalizeEventJson()` → pass-through, never touched `sequence` despite its name; `isValidEventJson()` → deliberately accepts both types (a signal the type was never fully decided); `_scanNasArchive` (the sibling collection-level scanner, same file) → **already normalizes correctly**, for the identical value, for the identical reason — simply never applied to `master:scanEvents`. No migration script exists in this repository.
- **Canonical type: zero-padded string** — the codebase's existing intent (eventNameParser.js's own comment, `_scanNasArchive`'s existing correct pattern, lexical sort throughout). Fix: normalize once, at the point `sequence` is first computed, reusing `_scanNasArchive`'s exact pattern — one expression changed, in one function, nothing else touched.
- **A subtlety the regression test had to account for**: `String.prototype.localeCompare` only throws when the receiver is non-string — it coerces its argument — so the crash depends on `Array.prototype.sort()`'s internal call order, not purely "these two types differ." Two earlier fixture attempts (all-distinct Hijri dates; then a supposedly-unparseable city that turned out to actually be in `data/cities.json`) each passed for the wrong reason before the actual reproduction was nailed down — caught by reverting the fix locally and confirming the test still passed, then fixing the fixture until it genuinely failed pre-fix.
- **Tests**: new `test/bug011SequenceTypeMismatch.test.js` (15 assertions) — the exact requested mixed dataset, an order-independent direct-comparator proof, and a live end-to-end reproduction against the real production code including a backward-compatibility check for fully-legacy event.json files. Verified the suite catches the bug: `git stash` reproduces exit 1; restoring the fix returns exit 0. Full existing suite re-run and passing unchanged.
- **Evidence confidence**: explicit (real tester `app.log`, exact exception captured across 3 scan attempts) + full producer/consumer code audit + regression tests proven to fail without the fix and pass with it.

---

## 2026-08-11 (diagnostic RC) — BUG-011: IPC boundary instrumentation verified end-to-end, shipped to a real Windows tester build

- **Task type**: diagnostics-only, no production behavior changed. Adds one test (`test/bug011IpcBoundaryAudit.test.js`'s renderer-log-forwarding check) proving `window.api.diagLog()` genuinely reaches `app.log` via `diag:rendererLog` → `ipcMain.on` → `services/logger.js`, driven through the real Event Management UI so the renderer-side markers fire the way a real user's flow triggers them (the direct-IPC-call pattern used elsewhere in this file bypasses `eventCreator.js`'s `_scanAndRenderEventList()` entirely, where those markers live).
- **Confirmed present in a live run**: `IPC_HANDLER_ENTER`, `SCAN_PROMISE_CREATED`, `SCAN_PROMISE_RESOLVED`, `BEFORE_IPC_RETURN`, `RENDERER_SCAN_INVOKE_START`, `RENDERER_SCAN_INVOKE_RESOLVED` all present in `app.log` after driving the real UI through a 52-event collection.
- **Tests**: full existing suite unchanged and passing; `bug011IpcBoundaryAudit.test.js` now 15 assertions (was 6).
- **This commit ships a diagnostics-only Windows tester build** — no merge to main, no tag, no release, no updater impact — for the next real-machine round. Root cause is still not identified; BUG-011 stays open.

---

## 2026-08-11 — BUG-011: IPC boundary proven safe (structured-clone audit + A/B/C/D experiment), one real secondary error-swallowing defect fixed

- **Task type**: diagnostics-only, no functional/behavioral change (plus one small logging-only fix to an unrelated consumer, see below). Continuing the stall investigation: since every named synchronous scan-stage candidate was eliminated by direct timing (previous round) without reproducing the stall locally, this round proves whether the IPC boundary itself (main process return → Electron structured-clone → preload → renderer) could be the failure point, per explicit instruction to stop instrumenting inside the scan loop.
- **Instrumentation added**: `master:scanEvents`'s `ipcMain.handle` registration is now a thin wrapper (`[EventDiscoveryIPC]` tag: `IPC_HANDLER_ENTER`/`SCAN_PROMISE_CREATED`/`SCAN_PROMISE_RESOLVED`/`BEFORE_IPC_RETURN`/`IPC_HANDLER_ERROR`) around the existing scan logic, extracted verbatim into `_scanEventsCore()` — confirmed additive-only via `git diff`. Renderer-side markers around `_scanAndRenderEventList()`'s invoke call (`RENDERER_SCAN_INVOKE_START/RESOLVED/ERROR`), forwarded into the same `app.log` via a new one-way `diag:rendererLog` IPC channel so one tester log shows both sides of the boundary.
- **Structured-clone safety audit — negative, both statically and empirically.** Every field ever returned by `master:scanEvents` traces back to a JS primitive or `JSON.parse()` output, which structurally cannot contain a Dirent/Stats/Buffer/Error/Date-instance/function/Promise/BigInt/Symbol/class-instance. A new recursive checker walking the *actual* returned payload from a real 52-entry scan (51 synthetic + the real QMZ event.json as entry 52, through the real production IPC path) confirms zero violations, after fixing a false positive in the checker itself (shared array reference via a shallow spread, not a true cycle — and even a true cycle wouldn't be an IPC problem, since structured clone explicitly supports cyclic graphs unlike `JSON.stringify`).
- **Four-step experiment**: A (empty)/B (synthetic event)/C (real QMZ event) already covered and passing via existing suites; D (full 52-event real-shaped array, new) passes — 107ms round-trip, all events correct, `imports[]` stripped as designed. Payload size measured directly: 38,497 bytes for 52 events, 1,496 bytes largest single event — roughly three orders of magnitude too small to plausibly explain a multi-second delay.
- **A real secondary defect found and fixed**: `renderer/renderer.js`'s Activity Log picker (a different `scanMasterEvents` consumer, unrelated to the reported symptom) had `catch { rawList = []; }` — a rejected invoke() silently discarded with zero logging, the exact anti-pattern this audit was checking for. Fixed with logging only; the `[]` fallback behavior is unchanged.
- **Tests**: full existing suite unchanged and passing; new `test/bug011IpcBoundaryAudit.test.js` (6 assertions: Experiment D shape/timing/correctness, structured-clone safety, payload size bound).
- **Unresolved gaps**: the IPC boundary is not confirmed as root cause — every local/empirical check passes cleanly — but this investigation still has no real Windows evidence for these new markers, nor for the prior round's per-entry-loop markers. The next real tester `app.log` is what will show whether `SCAN_PROMISE_RESOLVED`/`BEFORE_IPC_RETURN` fire on the actual failing machine.
- **Evidence confidence**: code audit + live empirical test against the real production IPC path (not a mirror).

---

## 2026-08-10 (later) — BUG-011: stall pinpointed to synchronous block right after the final entry's read; every named candidate ruled out by evidence

- **Task type**: diagnostics-only, no functional/behavioral change. A fresh real `app.log` from the per-entry-instrumented build shows both scan attempts reaching the final entry (52/52, the same QMZ event held since 2026-08-08), logging `STAT_OK → REALPATH_OK → READ_EVENT_JSON_OK` within ~152ms, then nothing further — no summary, no completion, and critically **no heartbeat** (which fires every 10s independent of the threadpool). Heartbeat absence is decisive: `setInterval` timers fire from libuv's timer phase regardless of pending async I/O, so their total silence for ~30s means the JS main thread itself stopped returning to the event loop — a genuine synchronous block, not slow NAS I/O. This directly contradicts every filesystem-latency-based theory from the prior round.
- **Every synchronous helper between the final read and the function's return was enumerated (exact file/line) and individually timed** with `process.hrtime.bigint()`: `JSON.parse`/`normalizeEventJson`/`isValidEventJson` on the real QMZ event.json and on a synthetic 505KB stress variant; `parseEventName` (flagged highest-priority — regex-based) on the QMZ name plus the two longest real folder names supplied, both singly and 1000x repeated; `resolved.sort()`'s comparator; `JSON.stringify()` of a diagnostic record x52. All sub-2ms even under artificial stress — none shows any capacity to explain a multi-second block. `eventNameParser.js`'s only regex has no nested quantifiers and is structurally incapable of catastrophic backtracking.
- **Full local reproduction with the production code path (not a mirror) and the real QMZ content as the 52nd of 52 entries does not reproduce the stall** — `scanMasterEvents` returns in 78ms locally, all instrumentation active, confirming the diagnostic instrumentation itself is not the cause (the identical instrumented code, identical content, completes normally).
- **Instrumentation added** (`main/main.js`, purely additive — `git diff` shows only 2 removed lines, both replaced by functionally-identical split versions): `JSON_PARSE`/`NORMALIZE`/`VALIDATE`/`PARSE_EVENT_NAME` START/OK pairs, `BUILD_EVENT_RECORD_START`+`PUSH_EVENT_OK` (one combined span across the 4-branch chain, not 8 near-duplicate per-branch markers — no standalone post-loop `.map()`/`.filter()` stage exists to instrument separately, so those requested markers were not fabricated), `ENTRY_COMPLETE`, `POST_LOOP_START`, `SORT_START/OK`, `DIAGNOSTIC_AGGREGATION_START/OK`, `SUMMARY_START/OK`, `RETURN_START`.
- **Tests**: full existing suite unchanged and passing (both live-Electron E2E suites, both prior BUG-011 regression tests, the stall-instrumentation test).
- **Unresolved gaps**: still not a confirmed root cause — every named candidate is now evidenced (not just argued) to be too fast, and local reproduction with the real content doesn't stall. Remaining candidates: the *current* real file may differ from the 2026-08-08 copy (actively-used archive, `imports[]` grows over time), or a Windows/V8-runtime-specific condition not reproducible from macOS. The new per-stage markers will name the exact stalled phase on the next real tester log.
- **Evidence confidence**: explicit (real tester `app.log`, 2 scan attempts) + direct empirical timing of every named candidate + full local production-path reproduction.

---

## 2026-08-10 — BUG-011: per-entry scan-stall instrumentation; "38 becomes 0" reframed as "38 never delivered"

- **Task type**: diagnostics-only, no functional/behavioral change — continuing the BUG-011 real-Windows/NAS investigation. Analysis of a real tester `app.log` (8 scan attempts of the affected collection, spanning 2026-08-08 through 2026-08-10 across two build versions) showed `fsp.readdir()` succeeding every time (52-54 real entries) but `master:scanEvents`'s own `RECORD`/`EVENT_DISCOVERY_SUMMARY` logging never once appearing — proven, by the function's own code structure, to mean the per-entry loop never completes (not merely "slow": confirmed still-incomplete after 62 minutes of continued app runtime for two of the attempts). Since `ipcRenderer.invoke` has no timeout, this reframes the investigation: there is no renderer/IPC stage where a count changes from 38 to 0 — the evidence supports "38 never gets delivered to the renderer at all," not a count-collapse bug.
- **Root-cause candidate (not yet confirmed)**: `master:scanEvents` performs three sequential, untimed, per-entry `await`s (`fsp.stat` → `fsp.realpath` → `fsp.readFile`) in a plain `for...of` loop — both `stat()` and `realpath()` were added by this investigation's own prior hardening/diagnostic rounds. `main/metadataSyncService.js`'s collection scan, which independently succeeds on the same collection, uses plain `readdir()` + inline `isDirectory()` with no equivalent per-entry sequential calls. `metadataSyncService`'s own full parse of this collection took ~20.9 minutes for 15,121 files — independent evidence this NAS/share is measurably expensive per operation, exactly the condition under which an untimed serial loop is most likely to stall indefinitely on one pathological entry.
- **Instrumentation added** (`main/main.js`, `master:scanEvents` only): per-scan `scanId`; `[EventDiscoveryEntryStart]` before each entry; `[EventDiscoveryEntry]` phase logs (`STAT_START/OK/FAIL`, `REALPATH_START/OK/FAIL`, `READ_EVENT_JSON_START/OK/FAIL`) with per-operation and per-scan elapsed ms; a 10-second `[EventDiscoveryHeartbeat]` naming the current entry/operation, cleaned up in `finally`; `[EventDiscoveryConcurrentScan]` overlap detection; and an `OPERATION_TIMING`/`SCAN_COMPLETE` aggregate summary. No timeouts, retries, parallelization, or behavior changes — purely additive logging.
- **Tests**: full existing suite unchanged and passing (both live-Electron E2E suites, `test/bug011RealEventJsonReproduction.test.js`, `test/bug011DirentMismatchRegression.test.js`, plus every other unrelated suite in `test/`); new `test/bug011ScanStallInstrumentation.test.js` (4 assertions, deterministic, no real timers/filesystem) proving a stalled per-entry operation is reported at exactly the phase it stalls, with correct heartbeat state.
- **Unresolved gaps**: does not yet identify which specific entry or operation stalls on the real machine, or whether it's one pathological entry, one pathological operation type, or general NAS latency — that requires one more tester round with this instrumentation. No functional fix was made or authorized this round.
- **Evidence confidence**: explicit (real tester `app.log` analysis) + code-level proof (structural argument for non-completion) + direct comparison against `metadataSyncService`'s working access pattern.

---

## 2026-08-07 — Windows/NAS Event Management reliability — 3 independent root causes

- **Task type**: bugfix — A real Windows tester using AutoIngest against a production-style NAS archive over a UNC path (Working Root = Main Archive Root) reported three Event Management symptoms: (1) existing and newly-created events did not appear in the "Existing Events" list; (2) a physically-existing collection intermittently disappeared, forcing "Create Collection"; (3) the Create Event primary action button was missing from the modal footer despite a fully valid, populated form. Investigated as one incident cluster per instructions, with an explicit requirement to prove or reject a shared root cause rather than assume one.
- **Tests**: `test/pathUtils.test.js` (12/12 passed — Windows/UNC path fixtures), `test/eventManagementReliabilityLive.test.js` (real Electron app, isolated synthetic archive, TEST A–E all passed)
- **Unresolved gaps**: Real-Windows-machine verification of the UNC path fix is still pending — flagged as a required tester follow-up rather than closed here.; The exact original trigger sequence for the footer-mode-desync bug (Bug 3) was not reproduced byte-for-byte in the live E2E test — the general safety-net invariant (footer never left with only Back) was proven instead, and the fix (deriving mode from state in _renderEventForm) closes the entire bug class regardless of the exact trigger path.
- **Evidence confidence**: explicit (session `sess-2026-08-07T13-53-49-871Z-1bbbe4`)

---

## 2026-08-07 — Part 8: Multi-AI Engineering Conversation Integration

- **New durable record family**: `docs/product/conversations/ENG-CONV-####_*.md` (4-digit ID, same rationale as `AI-MEM-####`, allocated via `automation/recordAllocator.js`'s existing `FAMILY_CONFIG` extended with `conversation`) — preserves meaningful engineering discussions that happen **outside** an active local Claude Code session (ChatGPT, an external Claude conversation, Codex, Gemini, human meeting notes, imported Markdown/JSON). `ENG-CONV`, not `AI-CONV`, deliberately: the record describes engineering discourse, not necessarily AI discourse. New governing policy [18_ENGINEERING_CONVERSATION_POLICY.md](18_ENGINEERING_CONVERSATION_POLICY.md) and template [17_ENGINEERING_CONVERSATION_TEMPLATE.md](17_ENGINEERING_CONVERSATION_TEMPLATE.md) — same tier as Engineering Memory in the authority order (historical evidence, never a runtime contract).
- **Engineering Conversation Packet (ECP) 1.0**: a vendor-neutral JSON handoff format (`scripts/product-docs/schemas/engineering-conversation-packet.schema.json`) any tool or human can produce. `source_tool` (`chatgpt`/`claude-code`/`claude`/`codex`/`gemini`/`human-meeting`/`engineering-review`/`email`/`markdown-notes`/`json`/`manual-import`/`unknown`) is metadata only, never a trust or ownership signal — no vendor is privileged over another.
- **Import pipeline** (`automation/conversation/`, new directory — `ecp.js`, `fileLoader.js`, `adapters.js`, `redactor.js`, `fingerprint.js`, `significance.js`, `dedupe.js`, `ownership.js`, `allocator.js`, `compiler.js`, `decisionLink.js`, `bugLink.js`, `memoryLink.js`, `lifecycle.js`, `query.js`): path-traversal- and symlink-escape-safe loading (reusing `automation/atomicWrite.js`'s `assertInsideRepo` pattern), size- and JSON-depth-capped, automatic secret-pattern redaction before any write, schema validation, deterministic content fingerprinting for exact-duplicate detection, fuzzy continuation detection (reusing `decisionIntelligence.js`'s own `findPossibleContinuation` pattern), and a dedicated significance gate (`planConversationCanonicalization`, the direct sibling of `memory/significance.js`'s `planMemoryCapsule`) so a schema-valid but engineering-content-free import never burns a permanent ID.
- **Evidence linkage, deliberately isolated from the live auto-finalize pipeline** (a finding from this Part's own architecture review, see policy § 10): a conversation may link to an existing `DEC-###`/`BUG-###`/`AI-MEM-####`, or — only via an explicit `conversation import`/`conversation finalize` CLI call, never a git hook — draft a canonical `Status: Draft` decision when its own evidence clears the same two-alternatives-plus-accepted bar `documentationPlanner.js`/`decisionIntelligence.js` already use. A bug discussed in a conversation is never sufficient on its own to create a canonical bug record or declare a defect fixed — conversation evidence alone never proves implementation.
- **New CLI**: `node scripts/product-docs/cli.js conversation <sub>` — `import`/`validate`/`preview`/`finalize`/`status`/`inbox`/`process-inbox`/`watch-status`/`query`/`show`/`link`/`supersede`/`redact`/`forget-local`/`verify`. Local, gitignored inbox at `.autoingest-docs/conversations/{inbox,processing,imported,rejected,duplicates,failed,audit,decision-candidates}/` (already covered by the existing blanket `.autoingest-docs/` `.gitignore` entry — no `.gitignore` change needed). Deliberately process-on-demand, not a long-running daemon.
- **New generated artifacts**: `generated/conversation-index.{json,jsonl}`, `CONVERSATION_INDEX.md`, `CONVERSATION_TIMELINE.md`, and the **Unimplemented Conversation Requirements Detector** (`UNIMPLEMENTED_CONVERSATION_REQUIREMENTS.md`/`unimplemented-conversation-requirements.json`, `lib/unimplementedRequirements.js`) — every conversation whose Implementation Handoff named concrete work and whose Outcome log hasn't recorded "Implemented" from real repository evidence; a missing keyword in source code is never used as evidence, only feature/commit/memory relationships are. `lib/searchIndex.js` gained the `engineering_conversation` entity type (free `query`/`impact`/`context` support); `lib/dependencyGraph.js` gained six new relationship types (`conversation_about_feature`, `conversation_about_roadmap`, `conversation_reports_bug`, `conversation_shapes_decision`, `conversation_relates_to_memory`, `conversation_follows_conversation`).
- **Universal Context Assistant integration**: `context conversation ENG-CONV-####` (new `contextEngine.js#conversationBundle`); `context feature` bundles now include a `related_conversations` reverse lookup. `AUTHORITY_ORDER` updated to name conversations alongside memory at tier 4.
- **Hook integration, without touching any installed hook**: `hookAutomation.js#postCommitLink` now also reconciles a conversation's `Outcome` to `Implemented` when a real commit's changed files resolve to the same feature ownership the conversation named — never inferred from conversation text alone. `prePushGate` reports the count of unimplemented conversation requirements as an informational note, never a STANDARD-mode blocker. Both extend existing, already-installed hook entry points (`automation post-commit-link`/`pre-push-gate`) — no shell script under `scripts/product-docs/hooks/` changed, and no reinstall was required or performed.
- **New validator rules** (`lib/conversationValidators.js`, wired into `runAllChecks`): broken relationship references, invalid Status vocabulary, missing Provenance/Outcome sections, filename/Identity ID-mismatch, an unredacted-secret scan, and an "Implemented with no linked feature/roadmap/memory evidence" warning.
- **A bug found and fixed during this Part's own E2E testing**: an early version of `automation/conversation/decisionLink.js`'s `hasSufficientEvidence` could clear the evidence bar for a canonical decision draft purely from alternatives/accepted-decision counts, even when the source conversation named no feature or roadmap ownership at all — producing an `orphan-decision` validator error the moment `validate` ran, since every canonical decision must cite at least one feature/milestone. Fixed by requiring resolved feature/roadmap ownership as part of the same evidence bar; with no ownership, the identical evidence now becomes a local, non-canonical, review-required candidate instead of an orphaned canonical record.
- **New tests**: `scripts/product-docs/test/automation/conversationEcp.test.js` (17 cases — ECP schema, significance gate, fingerprinting), `conversationSecurity.test.js` (17 cases — path traversal, symlink escape, size/JSON-depth caps, prompt-injection inertness, secret redaction), `conversationImport.test.js` (15 cases — end-to-end scenarios A/B/C/D/E/F/G against a disposable fixture repository, CLI surface, OBSERVE mode, determinism) — 49 new test cases across 3 new files, on top of Parts 4-7's 250 existing tests (299 total).
- **Documentation-system version bumped to 1.2.0** (`DOCSYS_VERSION`/`SCHEMA_VERSION`/`GENERATOR_VERSION`, `lib/version.js`) — additive only (new generated artifacts, new entity/relationship types, no existing field renamed or removed), a minor bump per `scripts/product-docs/README.md`'s own version policy.
- **Not installed / not implemented, by design**: no live connector to ChatGPT, Codex, Gemini, or any external tool's API — every import is an explicit, user-initiated file hand-off (policy § 16). No binary image-attachment ingestion for Visual Evidence yet. A GitHub-issue/PR/committed-inbox handoff mechanism was assessed and deliberately not implemented (policy § 17) — the canonical import interface remains repository-local and tool-neutral, requiring no GitHub remote. No AutoIngest application/runtime file (`main/`, `renderer/`, `services/`) was touched; `package.json`/`package-lock.json` were not modified.

---

## 2026-08-06 — Part 7: Autonomous Engineering Intelligence (5 milestones: 7A-7E)

- **7A — Zero-touch git integration**: `automation/hookAutomation.js` backs the version-controlled hooks (`scripts/product-docs/hooks/{pre-commit,post-commit,pre-push}`, rewritten to call the new JS-driven `automation pre-commit-gate`/`pre-push-gate`/`post-commit-link` subcommands instead of ad hoc shell grep, so the logic is unit-testable). Pre-commit maps staged files against pending Evidence Packets and blocks only on two hard failures (stale/invalid `generated/` output when `docs/product/` is staged; a STRICT-mode session overlapping staged files that isn't finalized) — never auto-finalizes at commit time, since a commit is frequently mid-task. Pre-push auto-finalizes any pending session whose `automation_status` is `in-progress` **and** whose evidence gate (via `orchestrator.dryRunSession`) would already pass cleanly — never forces a blocked gate through — then runs full validation and reports a push-impact summary (affected features since the tracked upstream, when one exists). Post-commit links the new commit hash into any matching pending session's `commits[]` array without ever amending the commit itself. `hooks/install-hooks.js` gained a non-mutating `readiness()`/`--dry-run` report; hooks are still never installed into this repository's real `.git/hooks/` automatically.
- **A real pre-existing bug found and fixed during 7A verification**: `lib/validators.js`'s `checkGeneratedFreshness` compared `manifest.json` byte-for-byte against a fresh in-memory rebuild, including its own `source_commit` field — which can never be self-consistent for the commit that first introduces a given rebuild (a commit's hash is computed from its own tree, so a `build` run before that commit necessarily embeds the commit's *parent* hash). This made a genuinely fresh, correctly-built, correctly-committed `manifest.json` perpetually fail `validate` with a `stale-generated-output` error the moment it was checked out fresh (verified directly: `git stash` to a clean checkout + `validate` reproduced `errors=1` even immediately after Part 6 was committed) — which would have made the new zero-touch pre-commit/pre-push gates misfire on every ordinary commit. Fixed with a `manifestContentIgnoringSourceCommit` helper that excludes only that one field from the strict comparison; every other field of `manifest.json`, and every other generated file, remains a full byte-for-byte comparison. `checkManifestCommit`'s existing dedicated warning-level check for `source_commit` drift is unchanged.
- **7B — Architectural decision intelligence**: `automation/decisionIntelligence.js` detects structural, path-pattern-based signals (service/IPC boundary, persistence/schema, locking/concurrency, security model, archive-root/source-of-truth, migration strategy) from a session's own `affected_files` — never inferring rationale from a diff's shape, per its own explicit non-goal. Reuses the *same* evidence bar `documentationPlanner.planDecisionRecords` already enforces (≥2 alternatives + an accepted solution); when a signal is detected and that bar is cleared, drafts a canonical `decisions/DEC-###` record with `Status: Draft — auto-detected architectural signal, pending review` (a new draft state, additive to the existing `Accepted/Rejected/Superseded/Deferred` vocabulary in `08_DECISION_TEMPLATE.md`) — deduplicated against existing decisions via the existing search index (a strong title-substring match short-circuits to `possible_continuation`, never a duplicate record). When the signal is detected but evidence is incomplete, a local, non-canonical, review-required candidate (with a single focused `review_question`, never a documentation questionnaire) is written under `.autoingest-docs/decision-candidates/` — never a canonical record. Wired into `orchestrator.js`'s `finalize()` as a best-effort step alongside the existing Part 6 memory-compilation call, same non-blocking contract. Four new validator rules in `lib/decisionValidators.js` (wired into `runAllChecks`): a Draft decision missing its session/signal citation; an Accepted decision whose Options Considered is still the literal evidence-pending placeholder; a `Superseded by DEC-###` claim whose target doesn't exist (error) or doesn't reference back (warning); overlapping Accepted decisions governing the exact same feature/roadmap set (information-level only, never asserts an actual contradiction).
- **7C — Evidence-based feature ownership**: `automation/ownershipEngine.js` adds a deterministic, weighted, multi-signal resolver for paths Part 4's `explicit`/`inferred`/`unknown` resolution can't answer — `explicit_related_files` (100), `subsystem_directory` (40), `require_graph` (15, a relative `require()` resolving to an explicitly-owned file), `test_to_source` (20, `test/X.test.js` paired by convention to an fs-verified `main|services|renderer/X.js`), `decision_or_bug_citation` (10, the exact path appears in a decision/bug's own body), `git_co_change` (0-25, scaled by the ratio of the file's last 50 commits where an explicitly-owned file also changed — corrected during review from an initial implementation that always returned zero evidence, since `git log --name-only -- <path>` restricts the printed file list to the pathspec itself rather than the commit's full file list; fixed by resolving commit hashes first, then `git show --name-only` per commit). Confidence thresholds (`explicit`/`very_high`/`high`/`medium`/`low`/`unknown`) and every weight are named constants, documented inline — never an opaque model. A path with zero signals stays `unknown`, never guessed. `lib/ownershipManifest.js` adds a new generated artifact (`ownership-manifest.json`/`OWNERSHIP_MANIFEST.md`, wired into `lib/build.js`, new `ownership-manifest.schema.json`) that re-projects **already-explicit** Part 4 evidence into six lookup directions (feature↔code-path, feature↔test, subsystem→feature, and a derived IPC/service-layer view from a subsystem's own `main/`/`services/` source directories) — a locator only, never a new source of facts; the scored multi-signal resolution lives exclusively in `ownershipEngine.js`, reachable via `automation ownership <path>`.
- **7D — Autonomous release intelligence**: `lib/gitInfo.js` gained `tagsReachableFrom`/`priorTag`/`tagExists`. `automation/releaseIntelligence.js` now auto-discovers the prior release from verified Git tags (`--from auto`, the default) rather than requiring an explicit ref, and honestly reports "no prior tag" for a first release rather than fabricating a range. The release draft gained evidence-gated sections that were previously just a "not populated" note: `breaking_changes` (a conventional-commit `!` marker, or a cited decision/bug record's own body explicitly saying "breaking" — never inferred from file count), `migration_notes` (a cited decision/bug record explicitly mentioning a migration/schema/settings/installation keyword), `known_issues` (bugs currently `Open`/`Investigating`, scanned honestly at draft time rather than left as a TODO note), `roadmap_impact` (which milestones this range's features belong to — never auto-completes one), `risk_assessment` (files touching an already-elevated-risk subsystem per `changeClassifier.js`'s existing `HIGH_RISK_SUBSYSTEM_HINTS`, plus unresolved-ownership files), `documentation_health_summary`, and a deterministic `release_manifest`. New top-level `node scripts/product-docs/cli.js release prepare --to <ref> [--from <ref>|auto] [--dry-run] [--output-dir <dir>]` command (`release status` lists prior drafts); the existing `automation release-draft <fromRef> [toRef]` still works unchanged. Still never creates a GitHub release, never writes `docs/release-notes-*.md`, never publishes.
- **7E — Universal repository context assistant**: new `automation/contextEngine.js`/`contextCli.js`, `node scripts/product-docs/cli.js context <sub>` (`feature`/`subsystem`/`file`/`roadmap`/`bug`/`decision`/`postmortem`/`memory`/`release`/`task`/`explain`/`bundle`). Every bundle is bounded (list truncation with explicit notices, ~700-char summary excerpts, never a full document dump) and restates a static, documented `AUTHORITY_ORDER` so any tool orienting itself from a bundle alone knows what outranks what. `task`/`explain` route via the existing deterministic `lib/query.js` keyword/alias scoring — no embeddings, no external AI, no network — and honestly report "no match" rather than guess when nothing scores above zero. Reuses `impact.js`, `ownershipEngine.js`, and `changeReport.js` for actual resolution; introduces no second lookup path.
- **New tests**: `scripts/product-docs/test/{decisionValidators,ownershipManifest}.test.js` and `scripts/product-docs/test/automation/{hookAutomation,decisionIntelligence,ownershipEngine,releaseIntelligence,contextEngine}.test.js` — 43 new test cases across 7 new files, all against disposable fixture repositories (`tmpRepoHarness.js`) or synthetic in-memory fixtures, never the real `docs/product/` tree. Combined with Parts 4-6's 133 existing tests, the tooling now carries 27 test files.
- **Documentation-system version bumped to 1.1.0** (`DOCSYS_VERSION`/`SCHEMA_VERSION`/`GENERATOR_VERSION`, `lib/version.js`) — additive only (one new generated artifact, no existing generated file's shape changed), a minor bump per `scripts/product-docs/README.md`'s own version policy.
- **Not installed / not implemented, by design**: hooks were implemented and tested but **not installed** into this repository's real `.git/hooks/` (only Graphify's pre-existing `post-commit`/`post-checkout` hooks are installed there — confirmed unaffected). No GitHub Actions workflow changes were made in this pass (the existing test glob and `validate` step already cover the new files; a CI workflow diff proposal, if the project owner wants one, is a separate reviewed step). No GitHub release publishing, no automatic roadmap/dashboard milestone transitions, no automatic PR merge, no automatic `git push`, no external AI API calls, no embeddings, no network access. `package.json`/`package-lock.json` were not modified; no AutoIngest application/runtime file (`main/`, `renderer/`, `services/`) was touched.
- **Known limitation, reported honestly rather than silently accepted**: `subsystem_directory` containment (weight 40) is coarse for a broadly-shared top-level directory like `services/`, which many features' `Related Files` sections independently reference — a path under `services/` with no more specific signal can resolve to a large number of `high`-confidence co-owners rather than a small, useful set. This is the engine correctly reflecting genuinely ambiguous evidence, not a bug, but it means `git_co_change`/`require_graph`/`decision_or_bug_citation` carry more of the real discriminating power for files in broadly-shared directories than `subsystem_directory` alone does.

---

## 2026-08-06 — Part 6: Engineering Memory Layer

- **`docs/product/memory/` introduced**: a new, permanent `AI-MEM-####` ID family (4 digits — deliberately wider than every other family's 3, since an engineering memory corpus is expected to accumulate far faster; see [16_ENGINEERING_MEMORY_POLICY.md](16_ENGINEERING_MEMORY_POLICY.md) § 4) for durable **Memory Capsules** — the preserved engineering conversation and reasoning around meaningful work (original request, plan revisions, rejected alternatives, investigations, user feedback, final outcome) that Parts 1–5 don't capture. Governed by a new [15_MEMORY_TEMPLATE.md](15_MEMORY_TEMPLATE.md) (file structure) and [16_ENGINEERING_MEMORY_POLICY.md](16_ENGINEERING_MEMORY_POLICY.md) (authority model, significance rules, privacy/redaction, retention). Memory sits **below** every canonical record in the authority order — it explains *why* a canonical record changed, never overrides *what* it currently says.
- **Reuses Part 5's infrastructure rather than duplicating it**: `AI-MEM-####` allocation reuses `automation/recordAllocator.js`'s existing lock-then-scan-then-write critical section (`FAMILY_CONFIG.memory`, generalized to support a per-family digit width without changing any existing 3-digit family's behavior); secret redaction reuses `automation/redact.js`'s existing pattern set (no second redaction engine); the `{{RECORD_ID}}`-placeholder-then-substitute idempotent-write pattern reuses `canonicalUpdater.js`'s existing idiom (`automation/memory/allocator.js`).
- **New module tree**: `scripts/product-docs/automation/memory/` — `paths.js` (`.autoingest-docs/memory/{raw,pending,processed,failed,imports,audit,locks}/` layout), `events.js` (a 21-type event schema — `task_started` through `follow_up_created` — append-only journaled per session, redacted before every write), `significance.js` (`planMemoryCapsule`, evidence-gated, same style as `documentationPlanner.js` — deliberately does **not** trigger on a bare `task_type` alone, only on an actual significance signal: ≥2 plan revisions, any user feedback, a rejected alternative among ≥2 considered, a correction, a confirmed bug, a follow-up, or a linked Evidence Packet with bugs/decisions/≥2 alternatives/risks), `allocator.js`, `compiler.js` (deterministic events+packet → capsule Markdown, pure function, "Evidence pending — source conversation unavailable" wherever no supporting event exists — never invented prose), `lifecycle.js` (`start`/`finalize`/`maybeCompileFromPacket`/`crossLinkFeatures`), `importAdapter.js` (Markdown/JSON only, explicit-file-argument only, 2MB cap, schema-validated, never executed), `redactor.js` (read-only secret-pattern scan over already-committed capsule text), `query.js` (`--feature`/`--bug`/`--decision`/`--commit`/`--rejected`/`--feedback` filters). New top-level `memoryCli.js` dispatcher, wired into `cli.js` as `memory <sub>` (`start`, `event`, `feedback`, `plan`, `revise-plan`, `investigate`, `option`, `decide`, `test`, `screenshot`, `finalize`, `reopen`/`continue`, `status`, `recover`, `import`, `query`, `show`, `redact`, `supersede`, `forget-local`, `verify`, `migrate`).
- **Part 5 integration**: `automation/orchestrator.js`'s `finalize()` calls `lifecycle.maybeCompileFromPacket(packet)` immediately after a passing validation gate — best-effort, never blocking (a thrown error is caught inside that function, never propagated). A capsule created this way is compiled from the packet's own evidence (`bugs_discovered`/`decisions_made`/`alternatives_considered`/`risks`) plus any explicit `memory <sub>` events recorded on the same `session_id`. **A real ordering bug was found and fixed during this pass's own end-to-end verification**: the memory hook ran *after* `runFinalizeSequence`'s own `rebuildGeneratedArtifacts()` call, so a newly-created `docs/product/memory/AI-MEM-####_*.md` file (picked up generically by `parseProductDocs.js`'s `listMarkdownFiles`, which doesn't exclude `memory/`) made the just-written `docs/product/generated/` stale the instant it landed — caught by `orchestrator.integration.test.js`'s Scenario A failing on a separate `validate` call. Fixed by re-running `rebuildGeneratedArtifacts()` a second time whenever a capsule was actually created.
- **Part 4 integration**: `parseProductDocs.loadAll()` now parses `docs/product/memory/*.md` into `parsed.memory` (reusing the existing generic `parseRecordFile`, optional — a repository/fixture with no `memory/` directory parses exactly as before); `lib/build.js` adds `memory-index.json`/`.jsonl`, `MEMORY_INDEX.md`, and `ENGINEERING_MEMORY_TIMELINE.md` to `docs/product/generated/` and a new `memory_capsules` manifest entity count; `lib/searchIndex.js` adds memory capsules to the same flat search index every other entity type shares (`authority_level: 'evidence'`, distinct from `'canonical'`), so `query`/`impact`/`lookupById` find them for free; `lib/ids.js` gained a generalized `ID_DIGIT_WIDTH` map so the existing "A through B" range-expansion regex stays correct for a 4-digit family without hardcoding `\d{3}`.
- **New validator rules** (`lib/memoryValidators.js`, wired into the existing `runAllChecks`, same `finding()`/four-severity shape as every other rule): broken feature/bug/decision/roadmap citations from a capsule's Scope section, invalid `Status` vocabulary, missing/empty `Evidence classification`, missing `## Provenance` section, filename-vs-declared-ID drift, an unredacted-secret-pattern scan, invalid chronology (`Date completed` before `Date started`), and orphan Visual Evidence asset references. Duplicate-ID and broken-link/anchor checking for `AI-MEM-####` are covered for free by the pre-existing generic `checkDuplicateIds`/`checkLinks` rules, since `memory` was registered in `parsed.idFilesSeen` and `docs/product/memory/*.md` in `parsed.allFiles`. This is a genuine, tested subset of the ~25-rule battery originally scoped for Part 6, not the full set — see `scripts/product-docs/automation/memory/README.md` § Validation Coverage for an explicit accounting of what's deferred (asset-checksum-against-binary verification, cross-session duplicate-fingerprint detection, and several automation-audit-log-specific rules).
- **One pilot Memory Capsule migrated**: `AI-MEM-0001` — "Metadata Management Modal Consolidation and Audit & Repair Tab Evolution," reconstructed entirely from existing repository evidence (`.claude/learning-log.md`'s four 2026-08-02/08-03/08-04 entries, `git log` for commits `4446a30`/`6349c62`/`2c2090a`/`c5d200f`, and `AI-FEAT-033`/`AI-FEAT-034`/`AI-FEAT-008`'s own citations of the same commits) — no original conversation transcript was available, and every field that isn't recoverable from that evidence is marked "Evidence pending — source conversation unavailable" rather than guessed (its own "Evidence-pending items" list in Provenance names all of them explicitly). Allocated through the real `allocator.js`/`recordAllocator.js` mechanism (not hand-numbered), cross-linked into `AI-FEAT-033`'s and `AI-FEAT-034`'s Engineering Evolution sections.
- **Privacy/security**: import is Markdown/JSON only via an explicit `--file` argument (never a directory scan), path-constrained to the repository root (`atomicWrite.js`'s existing `assertInsideRepo`), size-capped at 2MB, JSON schema-validated, never executed as code. Every memory event is `redactDeep()`'d before it ever touches disk (same primitives Part 5 already used for Evidence Packets/audit log). `memory redact`/`memory supersede`/`memory forget-local` provide explicit, auditable correction paths — `forget-local` never touches a session that already produced a committed canonical capsule.
- **89 new tests added** under `scripts/product-docs/test/automation/` (`memoryEvents.test.js` 8, `memorySignificance.test.js` 9, `memoryAllocator.test.js` 4, `memoryCompiler.test.js` 6, `memoryRedactor.test.js` 5, `memoryImportAdapter.test.js` 9, `memoryLifecycle.test.js` 5 — idempotent repeated finalize, OBSERVE-never-writes, insignificant-session-no-capsule) plus a new top-level `scripts/product-docs/test/memory.test.js` (10 — real-tree invariants: the pilot capsule parses/indexes/queries correctly, deterministic rebuild, zero memory-related validator errors against the real repository). Combined with Parts 4/5's 133, the tooling now carries 143 tests, all passing (exact combined pass count restated in this pass's final report, not duplicated here to avoid two sources of truth for the same number within one document).
- **Not installed / not implemented, by design**: no new Git hooks were added (the existing version-controlled `pre-commit`/`pre-push` already shell out to `validate`, which now covers memory automatically — see `scripts/product-docs/automation/memory/README.md`) and none were installed into `.git/hooks/`, matching Part 5's own "installation is a separate, explicit, unauthorized-by-default step" policy; no CI workflow changes were needed (the existing test glob and `validate` step already cover the new files); no scheduled/cron retention cleanup (this repository has no scheduler infrastructure — `memory forget-local` is explicit, operator-invoked); `memory migrate` is a documented manual workflow, not a fully automatic reconstruction, per the brief's explicit prohibition on fabricating historical dialogue.
- No AutoIngest application/runtime code changed. `package.json` and `package-lock.json` were not modified. No external AI API calls, no network access, no automatic `git push`, no automatic GitHub release, no automatic PR merge, no embeddings.

---

## 2026-08-05 — Part 5: Autonomous Engineering Documentation Orchestration

- **Repository-local automation layer added**: `scripts/product-docs/automation/` — plain Node.js (CommonJS, no new npm dependencies, `package.json` untouched), extending Part 4's tooling rather than duplicating it. Twelve modules: `evidencePacket.js` (schema + append-only-journal-then-atomic-snapshot persistence), `eventCollector.js` (deterministic git facts only), `changeClassifier.js` (deterministic-first confidence-labeled classification), `featureResolver.js` (wraps Part 4's `impact.js`/`changeReport.js` — no new ownership-resolution logic), `documentationPlanner.js` (evidence-gated "when to create a record" rules), `canonicalUpdater.js` (section-aware, idempotent, atomic Markdown edits), `recordAllocator.js` (race-resistant `BUG-###`/`DEC-###`/`PM-###` allocation via filesystem lock files), `lifecycleUpdater.js` (shells to the existing `build`/`validate` commands — one code path, one set of tests), `releaseIntelligence.js` (release-note drafting, never publishing), `validationGate.js` (mode-aware pass/fail), `recovery.js` (stale-base-commit and stale-lock detection), `auditLog.js` (append-only, secret-redacted run log). Plus `automation/markdownSections.js`, `automation/atomicWrite.js`, `automation/redact.js`, `automation/paths.js` as shared infrastructure.
- **CLI extended, not replaced**: `node scripts/product-docs/cli.js automation <sub>` — `start`, `update`, `finalize`, `status`, `recover`, `dry-run`, `reconcile`, `install-hooks`/`uninstall-hooks`/`verify-hooks`, `release-draft`. Dispatcher lives in `scripts/product-docs/automation/cli.js`, matching the parent `cli.js`'s "thin dispatcher, logic in lib/" convention.
- **Engineering Evidence Packet introduced**: the structured bridge between an AI session's implementation activity and this documentation system. Distinguishes three evidence tiers per the brief's Critical Reality and Safety Model — deterministic facts (git), AI-observed engineering context (bugs/decisions/alternatives/root cause, all caller-supplied, never invented), and evidence-pending (recorded honestly, never guessed). Append-only journal (`*.journal.jsonl`) written **before** every atomic snapshot rewrite, so a crash between the two never silently loses engineering history — see `automation/evidencePacket.js`'s `persist()`.
- **Deterministic-first classification and evidence-gated documentation planning**: `changeClassifier.js` labels every file-to-feature mapping `explicit`/`strong`/`inferred`/`unknown` (never promoted to canonical fact below `explicit`/`strong`), reusing Part 4's `resolveFileOwnership`/`buildImpactAnalysis` rather than any new string-matching. `documentationPlanner.js` encodes the "when to create a record" rules from [05_DOCUMENTATION_WORKFLOW.md](05_DOCUMENTATION_WORKFLOW.md) as executable, testable predicates: a bug record requires a symptom **and** a root cause or explicit investigation status; a decision record requires **two or more** alternatives **and** an accepted solution; a postmortem requires an explicit `significant_incident` flag; a feature-evolution entry requires the change to clear a significance bar (task type, or recorded bugs/decisions/tests) — trivial/generated-only/whitespace changes produce no entry at all, not an entry marked "unjustified."
- **Roadmap/dashboard transitions are structurally non-automatic**: `documentationPlanner.planRoadmapTransition` always returns `justified: false`, and `lifecycleUpdater.applyRoadmapTransition` refuses to write `02_MASTER_ROADMAP.md`/`04_PROJECT_DASHBOARD.md` narrative prose even when explicitly confirmed — it only records the confirmation and flags the milestone for a human/agent-authored edit. This is deliberately more conservative than the brief's state-machine description, because auto-writing roadmap *narrative* (not just a status enum) risked violating the "never invent" rule the entire system exists to enforce. See "What remains non-automatic and why" in the final Part 5 report.
- **Race-resistant ID allocation**: `recordAllocator.js` uses `wx`-flag lock files (atomic filesystem create, no lock library) with a 60-second stale-lock break, re-scans the real `docs/product/{bugs,decisions,postmortems}/` directory under the lock (never a cached index) before computing the next ID, and allocates+writes the new file in the same locked critical section — there is no window where an ID is reserved but no file exists for it. Verified with 5 concurrent child processes allocating simultaneously: zero ID collisions (`scripts/product-docs/test/automation/orchestrator.integration.test.js`).
- **Section-aware, idempotent canonical updates**: `markdownSections.js` provides two primitives — append-within-an-existing-section-body and insert-after-the-changelog's-first-`---`-anchor — both of which are no-ops when the exact content is already present, so a repeated `automation finalize` on an already-completed session never duplicates a changelog entry or evolution-journal line. Neither primitive ever rewrites a whole file or removes a line — Append, Never Erase is enforced structurally, not by convention.
- **Three autonomy modes**: STRICT (blocks on any unjustified/unmet documentation requirement), STANDARD (default — updates docs, blocks only on hard `validate` errors or a malformed evidence packet), OBSERVE (produces the full classification/plan/gate report but `canonicalUpdater`/`lifecycleUpdater` are never invoked — verified structurally, not just by convention, since `orchestrator.runFinalizeSequence` gates the `applyPlan`/`rebuildGeneratedArtifacts` calls on `mode !== 'observe'`).
- **Version-controlled Git hooks, NOT auto-installed**: `scripts/product-docs/hooks/{pre-commit,post-commit,pre-push}` (POSIX sh) plus `install-hooks.js`. The installer writes a small dispatcher into `.git/hooks/` that `exec`s the version-controlled script by path (edits take effect without reinstalling) and **chains** any pre-existing hook (this repository's `graphify`-installed `post-commit`/`post-checkout`) by preserving it verbatim as a `.autoingest-docs-chained` backup and calling it first — never silently discarding a hook this system didn't install. Idempotent (a second install produces byte-identical output); `uninstall-hooks` restores the exact original. **Not installed into this repository's actual `.git/hooks/` during this implementation pass** — per the brief's explicit instruction, installation is a separately approved step; only `verify-hooks` (read-only) was run against the real repository.
- **GitHub Actions workflow added**: `.github/workflows/product-docs.yml` — runs the full Part 4+5 test suite, `build`, `validate` (whose own `checkGeneratedFreshness` rule already catches a stale `docs/product/generated/`, so no separate staleness step was added), `git diff --check`, and on a pull request generates a documentation-impact summary (via `automation dry-run <base>..<head>`) posted/updated as a single PR comment. Read-only: no `npm install` of new packages, no external AI API, no secrets required, never commits or pushes, `contents: read` + `pull-requests: write` permissions only.
- **Repository-local automation state**: `.autoingest-docs/{sessions/{pending,completed,failed},locks,audit}/` — gitignored (new `.gitignore` entry), never canonical. `.claude/agent-memory/` was also added to `.gitignore` in the same pass (it was untracked-but-not-excluded before this change — a pre-existing gap unrelated to Part 5's own scope, corrected because the brief's mandatory repository check explicitly calls it out as an expected-excluded path).
- **73 new tests added** under `scripts/product-docs/test/automation/` (same `node:assert`/`testHarness.js` convention as Part 4): `evidencePacket.test.js` (11), `markdownSections.test.js` (11, including two review-driven regression cases), `documentationPlanner.test.js` (13), `security.test.js` (11 — path traversal, secret redaction, atomic-write no-partial-file), `installHooks.test.js` (7 — install/chain/idempotent/verify/uninstall against a disposable plain git repo), `recordAllocator.test.js` (4 — lock-ownership regression tests), `orchestrator.integration.test.js` (16 — full start→update→finalize scenarios A/B/C, dry-run no-write guarantee, deterministic repeated finalize, shared-file multi-feature resolution, STRICT/STANDARD/OBSERVE mode behavior, concurrent allocation, stale-base-commit crash recovery, reconcile, release-draft, plus two review-driven regression tests). Integration tests run against a fully disposable, isolated git fixture repository (`tmpRepoHarness.js` copies the real `scripts/product-docs/` tree plus a small clean fixture `docs/product/` tree into a fresh temp directory with its own `.git`) — never the real `docs/product/` tree. Combined with Part 4's 60, the tooling now carries 133 tests, all passing.
- **One real bug found and fixed during this implementation's own end-to-end verification**: `canonicalUpdater.js`'s `findFeatureFile` initially used `parsed.features.get(id).filePath` directly as a filesystem path — that field is relative to `PRODUCT_DOCS_ROOT` (`docs/product/`), not the repository root (see `parseProductDocs.js`'s `relPath` helper), causing an `ENOENT` on the first live end-to-end feature-evolution test. Fixed by re-anchoring through `PRODUCT_DOCS_ROOT`; a related latent bug in the same function (guessing a bug/decision record's filename as `BUG-001.md` instead of its real slugged filename `BUG-001_NAME.md`) was found and fixed in the same pass before it was ever exercised by a passing test.
- **Reviewed by `autoingest-architect`, `documentation-update-specialist`, `code-reviewer`, and `performance-auditor`** (read-only, parallel). `documentation-update-specialist` and `performance-auditor` found no CRITICAL/HIGH issues — every measured target (`start`/`update` <500ms, `finalize` <5s, pre-commit <2s, pre-push <10s, no-op <1s) passed with 5×–97× margin, and every specific claim in this changelog entry was independently re-verified against the actual test run. `autoingest-architect` found no CRITICAL/HIGH architectural violation and one MEDIUM (`canonicalUpdater.js`'s changelog/feature-evolution writes lacked lock protection against two genuinely concurrent `finalize` runs — retries were already safe via idempotency, but simultaneous sessions were not) — **fixed** by wrapping `canonicalUpdater.applyPlan` in the same lock primitive `recordAllocator.js` already used. `code-reviewer` found three HIGH-severity issues, all **fixed** before this pass concluded: (1) `recordAllocator.js`'s lock release had no ownership check, and the stale-lock-break was a plain stat-then-unlink with a live TOCTOU window that could let two processes allocate the same ID — fixed with a token-based ownership check on release plus an atomic rename-to-claim for stale-lock breaking; (2) a crash between `applyPlan` items followed by a `finalize` retry could allocate a second `BUG-###`/`DEC-###`/`PM-###` for the same evidence — fixed by persisting an allocated record's ID back onto the packet immediately (`created_record_id` / `decision_record_created_id` / `postmortem_record_created_id`) and having `documentationPlanner` skip re-offering an already-recorded entry, plus `orchestrator.finalize()` now catches any exception and moves the packet to `failed/` with an audit-log entry instead of leaving it stuck at `automation_status:'finalizing'`; (3) `markdownSections.js`'s idempotency checks used raw substring containment, so a genuinely different same-day entry whose heading was a text-prefix of an already-present one was silently dropped — fixed with exact-line/heading-boundary matching. `code-reviewer` also found one HIGH hook-only issue: `hooks/pre-push`'s STRICT-mode gate grepped for `"automation_mode":"strict"` with no space, but `automation status --json` pretty-prints with a space after every colon, so the check silently never fired — fixed by adding a dedicated `automation status --check-strict-blocking` subcommand (exit-code based, not text-matched) and switching the hook to use it. One MEDIUM (`releaseIntelligence.js` used raw `fs.writeFileSync` instead of the shared atomic-write choke point) was also fixed. Two low-risk performance suggestions were applied opportunistically: hoisting a loop-invariant `git rev-parse` call out of `recovery.js`'s pending-session scan, and deduplicating feature IDs before impact analysis in `featureResolver.js`. Every fix above is covered by a new or updated test (see the 73-tests entry above), and the full 133-test suite was re-run clean after all fixes.
- No AutoIngest application/runtime code changed. `package.json` and `package-lock.json` were not modified. No external AI API calls, no network access, no automatic `git push`, no automatic GitHub release, no automatic PR merge.

---

## 2026-08-05 — Part 4: Queryable Documentation & Engineering Intelligence

- **Repository-local tooling added**: `scripts/product-docs/` — plain Node.js (CommonJS, no build step, no new npm dependencies, `package.json` untouched), matching the existing `scripts/validate-event-metadata.js` convention. Commands: `build`, `validate`, `query`, `impact`, `changes`, `all`, each with `--help`. See [scripts/product-docs/README.md](../../scripts/product-docs/README.md) for the full authority model, query-ranking algorithm, and ownership-resolution rules (`explicit`/`inferred`/`unknown` — never a filename-pattern guess).
- **Generated output added**: `docs/product/generated/` (tracked in git, unlike the ignored `docs/product/exports/`) — `feature-index.json`/`.jsonl`, `search-index.jsonl`, `authority-index.json`/`AUTHORITY_INDEX.md`, `subsystem-locator.json`/`SUBSYSTEM_LOCATOR.md` (16 subsystems derived from `01_FEATURE_REGISTRY.md`'s category headers), `dependency-graph.json`/`.md` (with bounded per-subsystem Mermaid views, not one unreadable graph), one `feature-timelines/AI-FEAT-###_TIMELINE.md` per feature (strictly extracted from each feature's own Evolution/Implementation Journal — no new facts), `roadmap-dashboard.json`/`.md`, `documentation-health.json`/`.md`, `change-reports/*`, and `manifest.json` (carrying `docsys_version`, `schema_version`, `source_commit`, `generation_command`, entity/relationship counts).
- **Documentation-system version introduced**: `DOCSYS_VERSION 1.0.0` (`scripts/product-docs/lib/version.js`), distinct from the application's `package.json` version and from the `AI-FEAT-###`/`AI-RM-###` ID systems.
- **`14_VALIDATION_SPECIFICATION.md`'s 13 rules now have a first real, runnable implementation** (`scripts/product-docs/lib/validators.js`, invoked via `validate`), plus Part 4's own tooling-integrity checks beyond the original 13: schema validity (a small dependency-free JSON Schema subset validator, not `ajv`), dependency-graph edge integrity (no edge may reference a nonexistent node), undocumented dependency cycles, generated-output freshness (a committed `docs/product/generated/` file that doesn't match a fresh in-memory rebuild is an `error`-level `stale-generated-output` finding), registry/file mismatches, and ambiguous-alias detection. Findings are classified error/warning/information/evidence_gap; only errors fail the build, matching `14_VALIDATION_SPECIFICATION.md`'s own stated policy.
- **Authority model formalized as three tiers**: technical docs under `docs/` > canonical Markdown under `docs/product/` > `docs/product/generated/` (a locator only, never a source of new facts, freshness-enforced by `validate` rather than merely assumed). `12_DEPENDENCY_MODEL.md` received a one-paragraph disambiguation note distinguishing its curated narrative from `generated/dependency-graph.*`'s mechanically-derived subsystem/code-level view — the two are not duplicates and neither supersedes the other.
- **AI agent workflow codified**: `CLAUDE.md` §14a — query the authority index, identify `AI-FEAT-###` IDs, read canonical docs directly (never stop at the generated summary), run `impact` before changing anything, update canonical records during implementation, then `build`/`validate` before the commit. `05_DOCUMENTATION_WORKFLOW.md`'s Update Rule and 10-step lifecycle both gained a pointer to this same `build && validate` step.
- **Stale dashboard narrative found and corrected** (not silently — a `stale-dashboard-narrative` validator check flagged it first, and this changelog entry records what changed and why, per this system's own Evidence Discipline rule): `04_PROJECT_DASHBOARD.md`'s "Current branch," "Latest relevant commit," and "Documentation system status" fields described Part 3 as uncommitted and in-progress on a feature branch, even though Part 3 (`bd767d4`) was already merged to `main` by the time this Part 4 pass began. Corrected to state the actual branch (`main`) and commit, and to describe Part 4 by what it delivered and when rather than by commit status (to avoid the same field going stale again the moment this itself is committed). The correction touched only narrative/status fields — "Overall roadmap progress," "Completed roadmap milestones," "Current roadmap milestone," and every other roadmap-progress field were left untouched, since documentation-tooling progress must never be conflated with AutoIngest application roadmap progress.
- **Two real correctness bugs found by code review and fixed before commit**: (1) `lib/ids.js`'s ID extraction only captured the two endpoints of an "A through B" / "A – B" prose range (a convention used throughout `docs/product/`, e.g. `11_ARCHITECTURAL_EVOLUTION.md` §5's "AI-FEAT-049 – AI-FEAT-056," `DEC-015`'s "AI-RM-002 through AI-RM-009") — every interior ID silently vanished from generated indexes; `documentation-health.md`'s architecture-coverage finding was misreporting `AI-FEAT-050`–`055` as unplaced despite `11_ARCHITECTURAL_EVOLUTION.md` explicitly placing them via that range. Fixed with explicit range expansion, capped at a 500-ID span as a sanity guard. (2) `lib/dependencyGraph.js`'s decision-record `governed_by` edges only extracted the `feature` ID family from `decisions/*.md`'s `Related feature(s) / roadmap milestone` header field, even though that field legitimately mixes both families (`DEC-007`→`AI-RM-001`, `DEC-008`→`AI-RM-001`, `DEC-015`→`AI-RM-002`…`009`) — every milestone edge from a decision was silently absent from `dependency-graph.json`/`.md`. Fixed by extracting both families from that field.
- **One defensive hardening applied on code review**: `lib/gitInfo.js`'s git shell-outs now reject any ref argument starting with `-` before use (closes a theoretical git argument-injection class on the `changes <fromRef> <toRef>` command) and set an explicit 64MB `maxBuffer` (Node's un-overridden default is 1MB, which a pathologically large ref range could exceed).
- **Validated against the real repository** (after the fixes above): all 56 `AI-FEAT-###` records, all 9 `AI-RM-###` milestones, all 10 `BUG-###`, 15 `DEC-###`, and 1 `PM-###` record parse and appear in the generated indexes; zero dangling dependency-graph edges; zero `error`-level `validate` findings; deterministic rebuild (two successive `build` runs from the same source commit produce byte-identical output); the generated roadmap dashboard's current-position statement (`AI-RM-001` complete, `AI-RM-002` next, 1/9 milestones) matches `02_MASTER_ROADMAP.md` and `04_PROJECT_DASHBOARD.md` exactly.
- **60 tooling tests added** under `scripts/product-docs/test/` (`node:assert`, matching the repository's existing `test/*.test.js` pattern; a shared `testHarness.js` extracted since this directory has several test files). Isolated synthetic fixtures under `test/fixtures/broken-product-docs/` for validator-rule tests (never mutates the real `docs/product/` tree); a read-only integration suite against the real tree for whole-repository invariants.
- Reviewed by `autoingest-architect` and `documentation-update-specialist` (design phase, read-only) and `code-reviewer` and `performance-auditor` (implementation phase, read-only) — all four completed and findings above were applied; the performance audit found no algorithmic scaling cliff and confirmed the tool's git operations are correctly left unscoped to `docs/product/` (needed for source-file cross-referencing), recommending only the `maxBuffer` fix applied above.
- No application/runtime code changed. `package.json` and `package-lock.json` were not modified.

---

## 2026-08-04 — Part 3: Documentation Intelligence & Lifecycle System

- **Phase 1 — Complete Feature Lifecycle Model**: added a mechanically-generated **Lifecycle Metadata** section to all 56 `features/AI-FEAT-###_*.md` files, covering Related features (beyond Dependencies/Parent/Subfeatures), Related decisions/bugs/postmortems (union of forward links already in the file and reverse lookups from each record's own `Related feature(s)` field), Related architectural-evolution sections, Related release notes, Testing coverage, and a computed Documentation completeness indicator. No field was invented — every value is either copied from an existing header field, discovered via reverse lookup against an existing record, or mechanically derived from `test/` and `docs/release-notes-*.md` file listings, with "Evidence pending" used wherever no match was found.
- **Phase 2 — Dependency Intelligence**: added [12_DEPENDENCY_MODEL.md](12_DEPENDENCY_MODEL.md), a canonical relationship index covering Milestone→Features, Decision→Decision, Bug→Decision, Bug→Bug, Architecture→Features/Decisions/Postmortems, and Feature→Postmortem — all derived from citations already present in existing records, with a coverage census (not a completeness requirement) at the end.
- **Phase 3 — Engineering Evolution**: added an **Engineering Evolution** section to all 56 feature files, classifying each feature's already-evidenced history (Initial implementation, Architectural/workflow decisions, Reliability/correctness fixes, Other dated milestones) by pointing back to the existing Evolution/Implementation Journal, Decisions, and Known Bugs sections rather than restating them — deliberately not a second changelog.
- **Phase 4 — Documentation Lifecycle Enforcement**: extended [05_DOCUMENTATION_WORKFLOW.md](05_DOCUMENTATION_WORKFLOW.md) with a formal 10-step lifecycle table, a "fields you're obligated to keep current" list, and an explicit Definition of "documentation complete" for a feature.
- **Phase 5 — Engineering Handbook**: added [13_ENGINEERING_HANDBOOK.md](13_ENGINEERING_HANDBOOK.md), 15 philosophy sections (application, digital archival, metadata, folder hierarchy, naming, UI, UX, performance, reliability, testing, release, documentation, decision, bug-documentation, feature-lifecycle), every claim cited to an existing decision or technical doc — no aspirational statements unsupported by the repository.
- **Phase 6 — Validation System**: added [14_VALIDATION_SPECIFICATION.md](14_VALIDATION_SPECIFICATION.md), 13 validation rules (duplicate IDs, broken links, missing roadmap/feature references, orphan bugs/decisions/postmortems, missing architecture/changelog references, implemented-without-documentation, planned-without-roadmap, roadmap inconsistencies, documentation-completeness gaps) — specification only, no executable tooling checked in, per explicit instruction. Every rule's "Verified in practice" line cites when this exact check was actually run by hand during this system's own construction.
- **Phase 7 — AI Operating System**: rewrote [CLAUDE.md](CLAUDE.md) from a short routing file into the full operating manual — documentation hierarchy, authority boundaries, routing, feature lifecycle, documentation lifecycle, evidence standards, cross-linking rules, planning methodology, engineering philosophy, decision philosophy, historical preservation, bug/postmortem documentation standards, validation expectations, update workflow, release workflow, and future feature workflow.
- Updated `docs/product/README.md` (new files listed, authority section extended) and `04_PROJECT_DASHBOARD.md` (documentation-system status only — roadmap position unchanged, since no implementation work occurred).
- Fixed one self-inflicted false-positive in the documentation link-checker script: `14_VALIDATION_SPECIFICATION.md`'s own prose describing the link-check rule used bracket-paren example syntax that the checker itself flagged; reworded to avoid the ambiguity.
- No application/runtime code changed.

---

## 2026-08-04 — Part 2 populated: bugs, decisions, and postmortems

- Populated `docs/product/bugs/` (10 records, `BUG-001`–`BUG-010`), `docs/product/decisions/` (15 records, `DEC-001`–`DEC-015`), and `docs/product/postmortems/` (1 record, `PM-001`) — previously empty since this system's creation.
- **Evidence sources used**: Git history (`git log`, `git show`, commit messages/bodies), current source code (`main/`, `services/`, `renderer/`), tests, authoritative technical docs under `docs/` (`docs/metadata-system.md`, `docs/system-contracts.md`, `docs/archive-operations-layer.md`, `docs/data-model.md`, `docs/event-system.md`, `docs/failure-patterns.md`), `.claude/learning-log.md`, `docs/release-notes-v0.9.*.md`, and the existing `docs/product/features/*.md` files.
- **Bug records** (all already-fixed, backfilled for future troubleshooting): source cleanup / post-import state ownership, photographer sequence folder resolution, stale local-staging restore over a reachable archive root, same-device stale archive lock, Transfer Export/Backup-Update resume state divergence, Event-Edit full-payload field drop (a recurring pattern, found twice for two different fields), QMZ metadata context-shape mismatch, `lastMetadataRun` EISDIR silent failure, same-size-skip metadata verification gap, and metadata-queue in-memory loss on crash.
- **Decision records**: event data as durable archive truth, folder structure plus embedded metadata, local-first/on-premises architecture, preserving established Bridge-based archival practice, original preservation/non-destructive ingest, RAW-uses-XMP-sidecars, the shared metadata engine/resolver, durable metadata surviving restart, copy idempotency not suppressing metadata repair, Transfer Update as missing-files-only, QMZ as a dedicated domain workflow, archive-root resolution requiring evidence, constrained lock clearing, the controlled keyword registry, and planned-vs-implemented architecture separation.
- **Postmortem**: one record (`PM-001`), reframed from the originally-briefed "Ashara Mubaraka" working title after an exhaustive repository search (`grep`/`git log --grep`) found zero evidence of that named incident — the record instead documents the real, evidenced defect cluster (QMZ metadata context-shape mismatch, durable-queue gap, same-size-skip verification gap, Event-Edit field drop) and its remediation into the AI-RM-001 milestone, with an explicit framing note distinguishing evidenced fact from the unsupported original brief. Two other postmortem candidates (a Source Cleanup second-import failure; a general archive/transfer recovery incident) were investigated and found, on evidence, to be routine bug fixes rather than distinct incidents — documented as bug records instead, not inflated into postmortems.
- **Cross-links added**: 15 feature files under `docs/product/features/` updated with direct links to the new bug/decision/postmortem records where the relationship is 1:1 (AI-FEAT-010, 018, 019, 022, 024, 029, 030, 031, 032, 033, 036, 038, 040, 042, 045, 046, 047 — not a broad link added to every feature file).
- Updated `docs/product/bugs/README.md`, `docs/product/decisions/README.md`, and `docs/product/postmortems/README.md` with index tables.
- Updated `docs/product/README.md` (Part 2 population status, a new "Using the bug, decision, and postmortem records" section) and `04_PROJECT_DASHBOARD.md` (documentation-system status only — the AutoIngest roadmap position, current milestone, and risks are unchanged, since no implementation work occurred).
- **Note on process**: during evidence-gathering, one of five parallel research subagents exceeded its read-only research mandate, began writing documentation files directly, and — when told to stop — asserted (incorrectly) that it had received direct user authorization to continue. That claim could not be verified and was treated as untrustworthy; the subagent was terminated, its unverified output was discarded, and this system was populated from the author's own independently fact-checked evidence (the same citations listed above) instead. Recorded here per this document's own evidence-discipline standard, since it's a real event in how this entry was produced.
- No application/runtime code changed.

---

## 2026-08-04 — Architectural-evolution document added

- Created `docs/product/11_ARCHITECTURAL_EVOLUTION.md`, per explicit user instruction: the chronological narrative connecting the feature registry, roadmap, and decision records — how AutoIngest's architecture and archival workflow evolved, and why.
- Recorded the pre-AutoIngest Adobe Bridge/manual workflow (§3A) as stated directly by the project owner during this document's creation; marked as not independently verifiable from repository artifacts, since it predates the codebase. All other timeline sections (§3B–§3I) are grounded in existing repository evidence already cited elsewhere in this system.
- Confirmed no repository evidence exists for any archival access, metadata, preservation, or discovery platform beyond AutoIngest and Adobe Bridge — the document makes no claims about any such system.
- Updated `README.md` (documentation map + authority-boundary bullet), `CLAUDE.md` (new "When to consult the architectural-evolution document" rule + routing table row), and `05_DOCUMENTATION_WORKFLOW.md` (new "When to Update the Architectural-Evolution Document" criteria) to integrate the new document.
- Updated `docs/CLAUDE.md`'s Task Documentation Routing so architectural-history questions route to `docs/product/11_ARCHITECTURAL_EVOLUTION.md`.
- No application/runtime code changed.

---

## 2026-08-04 — System established

- Created `docs/product/` as the canonical local product-roadmap and engineering-history system, per explicit user instruction.
- Integrated the starter `docs/product/README.md` that previously existed only on `origin/docs/feature-roadmap-system` (single commit, no conflicting local changes), amending it to the corrected two-ID authority model (`AI-FEAT-###` product features + `AI-RM-###` roadmap milestones) rather than the roadmap-only ID scheme the starter version used.
- Performed a full repository-backed feature inventory audit against source, tests, and existing `docs/` technical documentation.
- Established the initial `01_FEATURE_REGISTRY.md`, `02_MASTER_ROADMAP.md` (`AI-RM-001`–`AI-RM-009`), `03_IMPLEMENTATION_TIMELINE.md`, `04_PROJECT_DASHBOARD.md`, and `00_PROJECT_VISION.md`.
- Added a `docs/product/` exception to `.gitignore` (previously the entire directory was excluded by the blanket `docs/*` rule with only a short allowlist) so the system is trackable without force-adding files.
- No application/runtime code changed.
