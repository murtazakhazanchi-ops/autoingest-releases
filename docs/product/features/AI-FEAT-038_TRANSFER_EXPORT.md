# AI-FEAT-038 — Transfer Export

| Field | Value |
|---|---|
| Feature ID | AI-FEAT-038 |
| Category | Transfer and Backup |
| Status | Implemented |
| Maturity | Stable |
| Parent feature | None |
| Subfeatures | AI-FEAT-040 (Backup Update Scanning) |
| Dependencies | AI-FEAT-042 (Archive Root Configuration) |
| Related roadmap milestone | None |
| Related technical docs | `docs/archive-operations-layer.md` § Transfer Workflow |
| Evidence status | Verified from docs (already fully read as required context) and current code |
| First-known implementation | Phase 13D era (2026-05-14, per `docs/archive-operations-layer.md`'s phase numbering) |
| Latest major update | Evidence pending beyond Phase 13D |

## Summary

Writes a clean mirror of selected events from the Active Archive Root to a Transfer Drive, for physical transport to the Main Archive Root.

## Current Behavior

`services/transferExportService.js`. No-overwrite contract (AutoIngest runtime artefacts — `.autoingest/`, locks, sync queue, tmp — are excluded from the mirror). Atomic per-batch checkpoint. Writes `.autoingest-transfer/exports.audit.jsonl` for traceability. Country is never part of folder names generated by export.

**Concurrency note** (per `autoingest-architect` review): `docs/archive-operations-layer.md` describes a "single-export-at-a-time lock," but a direct grep of `services/transferExportService.js` for lock/guard patterns did not confirm this shares any mechanism with AI-FEAT-045's `archiveLockService.js` (which is SHA1-keyed with TTL+heartbeat, scoped to event/photographer editing). Treat Transfer Export's concurrency guard as a **distinct, unconfirmed mechanism** — do not assume it's the same lock system as AI-FEAT-045 until independently verified.

## Original Plan / Intent

Evidence pending beyond the Phase 13D documentation already read.

## Evolution / Implementation Journal

- **Phase 13D (2026-05-14)** — introduced as part of the Archive Operations Layer milestone (`docs/release-notes-archive-operations.md`, `docs/archive-operations-layer.md`).
- **Backup Update Scanning** (AI-FEAT-040) was added as a sibling capability within the same service file, with its own conflict semantics.
- **Transfer Background/Minimize Operation** (AI-FEAT-041) added the ability to run this in the background without blocking the UI.

## Known Bugs / Troubleshooting

None recorded in `docs/product/bugs/` yet.

## Decisions

None recorded.

## Future Enhancements

None recorded — `docs/archive-operations-layer.md` § Known Limitations notes "No bulk adoption: events are adopted one at a time" (relevant to AI-FEAT-046, not this feature directly).

## Related Files

- `services/transferExportService.js`
