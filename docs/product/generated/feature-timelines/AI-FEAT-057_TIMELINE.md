# AI-FEAT-057 — Timeline

> Generated artifact — strictly extracted/reformatted from [features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md](../features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md)'s own Evolution / Implementation Journal, header table, Known Bugs, and Decisions sections. No new facts. Regenerate with `node scripts/product-docs/cli.js build`.

**Feature**: Multi-Channel Release & Update System

| Date | Event type | Summary | Related IDs | Confidence | Evidence source |
|---|---|---|---|---|---|
| 2026-08-12 | UI/UX revision | Initial implementation. Audited the existing release system (one workflow, one version field, one publish config — every RC/Stable distinction would have had to be invented from scratch). Verified electron-updater/electron-builder's real channel mechanics directly from their installed source rather than assumed. Investigated exact-binary RC→Stable promotion and rejected it (see DEC-017) in favor of rebuild-from-verified-source. Implemented settings/updater/release-gate/CI changes; added `scripts/product-docs/lib/updateChannelModel.js` and its test coverage. The live pilot (an actual `workflow_dispatch` RC run) was explicitly deferred to a separate, later-authorized activation step. | DEC-017 | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md § Evolution / Implementation Journal |
| 2026-08-12 | initial implementation | First-known implementation of Multi-Channel Release & Update System | — | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md header table: First-known implementation |
| 2026-08-12 | redesign | DEC-017 — Stable Releases Rebuild From Verified-RC Source; Never Promote Exact Binaries | DEC-017 | verified | decisions/DEC-017_STABLE_RELEASES_REBUILD_FROM_VERIFIED_RC_SOURCE_NEVER_PROMOTE_EXACT_BINARIES.md header table: Date |
| 2026-08-12 | redesign | DEC-018 — Part 9 — Multi-Channel Release & Update System: design and implementation | DEC-018 | verified | decisions/DEC-018_PART_9_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM_DESIGN_AND_IMPLEMENTATION.md header table: Date |
| 2026-08-13 | reliability improvement | Live CI pilot. Triggered the real `rc-build` `workflow_dispatch` path for the first time (`build_type: rc`, `rc_version: 0.9.12-rc.1`). `rc-tag` correctly created and pushed `v0.9.12-rc.1` from the exact HEAD commit (`a507c47`), with no unrelated feature work. `rc-build-mac` succeeded and published (8 mac assets + `rc-mac.yml`, `prerelease:true`). `rc-build-windows` **failed** at the electron-builder publish step — see [BUG-015](../bugs/BUG-015_WINDOWS_RUNNER_DEFAULT_POWERSHELL_SHELL_MANGLES_ELECTRON_BUILDER_DOT_NOTATION_CLI_OVERRIDES.md) for the full root-cause investigation (windows-2022's default `pwsh` shell mangled the `-c.publish.channel=rc` CLI argument). Verified live, at the moment of this partial failure, that Stable isolation held regardless: `v0.9.12-rc.1`'s GitHub release remained `prerelease:true`, `/releases/latest` continued resolving to `v0.9.11`, and Stable's `latest.yml`/`latest-mac.yml` were confirmed byte-identical to their pre-pilot state. Fixed the root cause (`shell: bash` added to the affected step, commit `66469f5`) and re-ran as `v0.9.12-rc.2` from that fix commit — both `rc-build-windows` and `rc-build-mac` succeeded, publishing a complete 12-asset RC release with both `rc.yml` and `rc-mac.yml` correctly scoped to `0.9.12-rc.2` only. `v0.9.12-rc.1` (mac-only, partial) was **not** deleted or altered — left in place as a real example of the rollback runbook's "partially uploaded RC" scenario, superseded by `v0.9.12-rc.2` per the runbook (see § Rollback / Recovery Runbook below). Ran the Stable promotion gate as a local dry run (no `v0.9.12` tag created): `release gate --channel stable --rc-commit 66469f5 --stable-commit 66469f5` (same commit) correctly `PASS`ed with zero drift; `release gate --channel stable --rc-commit 66469f5 --stable-commit 942bce1` (a deliberately unrelated earlier commit) correctly `BLOCK`ed, listing 51 drifted files. A final security/architecture/code review pass against the real pilot diff found no CRITICAL/HIGH issues (see Decisions section below for the one accepted structural note). | BUG-015 | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md § Evolution / Implementation Journal |
| 2026-08-13 | UI/UX revision | (later, same day) Forensic bootstrap audit, prompted by the discovery that the originally-written tester instructions were impossible: v0.9.11 (the tester's already-installed Stable build) predates commit `dfb9180` (Part 9's own introduction) and structurally cannot self-select Preview — verified directly (`git merge-base --is-ancestor v0.9.11 dfb9180`, zero occurrences of `updateChannel`/`getUpdateChannel` anywhere in v0.9.11's shipped `services/settings.js`/`renderer/index.html`, and `electron-updater`'s `allowPrerelease` is fixed permanently at construction time from the binary's own compiled-in version string). Concluded: manual one-time RC installation is a real, safe, already-functional bootstrap for a small number of designated testers (proven: identical `appId`/`productName` across versions means an in-place NSIS upgrade, not a side-by-side install; identical `<userData>/settings.json` path means existing settings survive; the missing `updateChannel` key correctly defaults to `'stable'` post-upgrade). No code change required — corrected the tester instructions instead. | — | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md § Evolution / Implementation Journal |
| 2026-08-13 | UI/UX revision | (later still) v0.9.12-rc.3 published solely to prove Preview→Preview automatic update, since rc.2 was already the newest Preview release and couldn't test the "discovers something *newer*" path. Deliberately built from the exact same application source as rc.2 (first attempt targeted `v0.9.12-rc.2`'s own commit `66469f5` directly via `--ref`; this failed at `rc-tag`'s tag-push step with GitHub's `workflows` permission restriction — `.github/workflows/release.yml` genuinely differed between that commit and `main`'s tip by one trivial `shell: bash` consistency line, and `GITHUB_TOKEN` blanket-blocks any push carrying a non-canonical workflow file regardless of the job's own `contents: write` grant. Re-dispatched from `main`'s tip instead — verified zero application-code difference from rc.2's exact commit via `git diff --stat` scoped to `main/`, `renderer/`, `services/`, `config/`, `package.json`, `package-lock.json`, and independently confirmed from `package.json`'s `build.files` allowlist that neither `docs/**` nor `scripts/**` nor `.github/**` are ever packaged into the app at all). Published cleanly: both platforms succeeded, 12 assets, `rc.yml`/`rc-mac.yml` correctly scoped to `0.9.12-rc.3`, Stable metadata re-verified byte-identical across all three RC publications to date. | — | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md § Evolution / Implementation Journal |
| 2026-08-13 | UI/UX revision | (closure) Real Windows-hardware evidence returned for the full lifecycle, across two separate tester sessions. First session (rc.2, on the manually-bootstrapped install): Stable isolation confirmed (v0.9.11 did not discover rc.2), manual bootstrap install succeeded with data/settings preserved, channel correctly defaulted to Stable post-install, Preview↔Stable switching and persistence both confirmed, no downgrade to v0.9.11 while running rc.2 on Stable. Second session (rc.3, Preview→Preview): rc.2 automatically discovered rc.3, downloaded and installed it through AutoIngest's own updater (not a manual install — independently evidenced by an in-app "Downloading update v0.9.12-rc.3... 100%" screenshot), Preview persisted through the update and the subsequent restart, and switching back to Stable afterward again produced no downgrade to v0.9.11. One tester-reported ambiguity was corrected by the tester themselves before this closure: an initial report of "no downgrade" behavior was first miswritten as a possible failure, then corrected to a confirmed PASS — no downgrade bug exists, and no code investigation or change was made on the basis of the miswritten first report, per the tester's own correction and this feature's evidence-discipline standard of never patching before root-causing a genuine, confirmed defect. See the Acceptance Matrix above for the complete evidence-tier breakdown. This closes Part 9's live-pilot verification — no further real-client testing is required for this feature's current scope. | — | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md § Evolution / Implementation Journal |
| 2026-08-13 — full live pilot closed. Three real RC publications (v0.9.12-rc.1/rc.2/rc.3), two CI-only bugs found and fixed (BUG-015, BUG-016), and real Windows-hardware verification of the complete Stable/Preview lifecycle: legacy isolation, manual bootstrap, in-place install with data preservation, Preview↔Stable switching, Preview→Preview automatic update, and no downgrade. See Acceptance Matrix below. | other dated milestone | Latest major update recorded for Multi-Channel Release & Update System | — | verified | features/AI-FEAT-057_MULTI_CHANNEL_RELEASE_UPDATE_SYSTEM.md header table: Latest major update |
| 2026-08-13 (commit `66469f5`) | major bug fix | BUG-015 — Windows Runner Default PowerShell Shell Mangles electron-builder Dot-Notation CLI Overrides | BUG-015 | verified | bugs/BUG-015_WINDOWS_RUNNER_DEFAULT_POWERSHELL_SHELL_MANGLES_ELECTRON_BUILDER_DOT_NOTATION_CLI_OVERRIDES.md header table: Fixed |
| 2026-08-13 (commit `fed471d`) | major bug fix | BUG-016 — Undeclared npm Dependency in product-docs Tooling Masked by Locally-Hoisted node_modules | BUG-016 | verified | bugs/BUG-016_UNDECLARED_NPM_DEPENDENCY_IN_PRODUCT_DOCS_TOOLING_MASKED_BY_LOCALLY_HOISTED_NODE_MODULES.md header table: Fixed |

